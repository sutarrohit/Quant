# @quant/prisma -- migrations only. Read by prisma.config.ts.
# The runtime connection lives in apps/server/.env; this one is for the CLI.

# --------------------------------------------------------------------------
# Postgres
# --------------------------------------------------------------------------
DATABASE_URL=                       # unpooled; migrations need a direct connection
