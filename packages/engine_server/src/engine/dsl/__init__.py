"""The strategy DSL: schema, hashing, indicator registry, interpreter, validator.

Strategy specs are **data**, never generated code. There is exactly one strategy
class and it interprets these models at runtime (spec section 0).
"""
