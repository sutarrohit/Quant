---
phase: 01-walking-skeleton-backtest-a-hand-written-spec-on-real-data
plan: 07
type: execute
wave: 4
depends_on: ["01-05"]
files_modified:
  - engine/pyproject.toml
  - engine/data/binance_vision.py
  - engine/data/catalog.py
  - engine/cli/ingest.py
  - engine/tests/data/test_binance_vision.py
  - engine/tests/data/test_catalog.py
  - engine/tests/data/fixtures/klines_ms_era.csv
  - engine/tests/data/fixtures/klines_us_era.csv
  - engine/tests/data/fixtures/klines_corrupt.zip
  - engine/tests/data/fixtures/klines_corrupt.zip.CHECKSUM
autonomous: true
requirements: [DATA-01, DATA-02, DATA-06]
user_setup:
  - service: aws-s3
    why: "D-23 puts the catalog in object storage from day one so both developers, CI and the cron read the same bytes — which is what makes success criterion 2's two-machine claim testable rather than aspirational"
    env_vars:
      - name: CATALOG_URI
        source: "the object-storage bucket path holding the catalog"
      - name: AWS_REGION
        source: "the bucket's region"
    dashboard_config:
      - task: "Create a bucket for the catalog and grant read/write to the developer identity and read to the CI identity"
        location: "AWS S3 console"

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The kline timestamp unit is detected per file from the raw value's digit count, so an ingest spanning the 2025-01-01 boundary produces correctly dated bars on both sides of it (DATA-06)"
    - "A full-history ingest for a symbol listed before 2025 produces a strictly increasing timestamp sequence with no jump to a nonsensical future date"
    - "Every bar in the catalog is timestamped at its close, asserted by a test against both the millisecond-era and microsecond-era fixtures (DATA-06, D-24)"
    - "A checksum mismatch hard-fails that file and exits non-zero; no code path falls back to an unverified copy (DATA-01, D-25)"
    - "Re-running ingestion skips files already downloaded and verified, so the operation is idempotent and resumable after an interruption (D-25)"
    - "Re-running a completed ingest writes no duplicate bars into the catalog and leaves the catalog byte-equivalent"
    - "The loader takes symbol, interval and date range as parameters; adding a symbol is a caller change, not a code change (D-21, D-22)"
    - "The catalog lives in object storage that CI and both developer machines read, with a local read-through cache so iteration does not pay an object-store round trip per read (D-23)"
    - "At least one ingested symbol has a materially later listing date than the others, so the loader's handling of a different range boundary is exercised now rather than assumed"
  artifacts:
    - path: "engine/data/binance_vision.py"
      provides: "hardened loader — per-file unit detection, monthly plus daily archives, resumable verified ingest"
      exports: ["download_klines", "verify_checksum", "parse_kline_row", "detect_timestamp_unit", "ChecksumMismatch", "IngestLedger"]
      min_lines: 140
    - path: "engine/data/catalog.py"
      provides: "object-store-backed catalog with a local read-through cache"
      exports: ["open_catalog", "write_bars", "write_instruments", "catalog_uri_from_env"]
    - path: "engine/cli/ingest.py"
      provides: "the ingest entry point — symbol, interval and range as parameters (D-21, D-22)"
      exports: ["main"]
    - path: "engine/tests/data/fixtures/klines_ms_era.csv"
      provides: "a checked-in 13-digit-timestamp sample so boundary parsing is testable without network access"
    - path: "engine/tests/data/fixtures/klines_us_era.csv"
      provides: "a checked-in 16-digit-timestamp sample for the other side of the boundary"
  key_links:
    - from: "engine/cli/ingest.py"
      to: "engine/data/catalog.py"
      via: "parsed bars are written to the catalog resolved from the environment, not from a hardcoded local path"
      pattern: "catalog_uri_from_env"
    - from: "engine/data/binance_vision.py"
      to: "engine/tests/data/fixtures/klines_us_era.csv"
      via: "the microsecond-era fixture is what proves the unit detection is real rather than a comment"
      pattern: "detect_timestamp_unit"
  prohibitions: []
---

<objective>
Take the tracer's single-symbol, single-month loader to real breadth: the timestamp-unit boundary
handled per file, full available history per symbol, a handful of majors with genuinely different
listing dates, resumable verified ingestion, and the catalog moved into object storage.

Purpose: the roadmap names ingestion as the largest single cost in this phase, and RESEARCH.md
found the one thing that makes it dangerous rather than merely tedious — the bulk kline files
silently changed timestamp units at 2025-01-01 with no marker of any kind, so a loader written and
tested against 2024 data misdates every 2025 bar without ever raising. A full-history ingest for
any symbol listed before 2025 crosses that boundary.
Output: a loader that is correct across the boundary, idempotent on re-run, and writing into a
catalog both machines and CI read.
</objective>

<scope_option>
## ⚠ SCOPE OPTION — this plan is the one both reviewers flagged as too thick for a walking skeleton

**This section is a decision for the developer, not a change already made.** The plan below is
unchanged and remains the default. Nothing here has been cut. What follows is a costed alternative,
recorded so that taking it is cheap and rejecting it is deliberate.

### What was flagged, and by whom

Both cross-AI reviewers independently said Phase 1 is broader than "the thinnest slice that still
exercises the whole spine", and both pointed at this plan. Codex (the source-grounded reviewer,
HIGH overall risk) cited `01-07-PLAN.md:227` — full available history, four symbols, object
storage, read-through caching, resumable ledgers and live AWS round trips — and observed that this
contradicts CONTEXT.md's own sentence in D-21: *"breadth arrives when Phase 3 needs it."* OpenCode
independently suggested local disk with an upgrade path instead of S3.

None of that is needed to prove one deterministic BTCUSDT backtest, which is what the phase goal
says Phase 1 is for.

### Option TRIM — what would stay, what would move

**Stays in Phase 1 (Task 1, entire):** per-file timestamp-unit detection and the 2025-01-01
boundary handling, the intra-file consistency check, close-time semantics with the negative
assertion, checksum hard-fail with no partial file left behind, the zip-entry escape check, the
`IngestLedger` resumable verified ingest, and all four offline fixtures. This is correctness work,
not breadth work: RESEARCH.md found the unit switch by live fetch, and a loader that misdates every
2025 bar without raising is exactly the kind of silent wrongness the phase exists to rule out.
Retrofitting it after bars are in the catalog means re-ingesting everything.

**Moves to a later data-expansion plan (Task 2):** the four-symbol full-available-history ingest,
the S3-backed catalog, the read-through cache, and the AWS bucket/credential setup. Phase 1 would
keep the tracer's local catalog, parameterised exactly as it already is — `catalog_uri_from_env()`
would still be written, still read the environment, and simply resolve to a local path.

### Consequences, stated plainly

**Requirement IDs: none leave Phase 1.** This is the part worth reading twice.
- `DATA-01` (checksum-verified ingestion) — stays, satisfied by Task 1.
- `DATA-06` (bar timestamp semantics asserted in tests) — stays, satisfied by Task 1.
- `DATA-02` (data stored in a catalog the engine reads natively) — stays, already satisfied by plan
  01-05's tracer, which writes and reads a real catalog. What moves is the *shared-across-machines*
  property, which `DATA-02` does not actually claim; that property comes from D-23, not from the
  requirement.

So this is **not** a roadmap change in the requirements sense. All 14 IDs remain in Phase 1.

**Three binding CONTEXT.md decisions would be deferred, and that is what makes it the developer's
call, not the planner's:**
- **D-21** — "BTCUSDT plus a handful of majors". Would become BTCUSDT only in Phase 1. The stated
  rationale for the handful is surfacing bugs a single symbol hides — differing precision filters,
  symbols that delist mid-range — which is real, and is the strongest argument against trimming.
- **D-22** — "1-hour bars, full available history per symbol". Would become the tracer's one month.
- **D-23** — "`ParquetDataCatalog` lives in S3 from day one". Would become local disk with the URI
  already parameterised, which is precisely OpenCode's suggestion.

**Success criteria: all six remain reachable, one changes shape.**
- Criteria 1, 3, 4, 5, 6 — unaffected.
- Criterion 2 (byte-identical on two machines) — currently plan 01-09's checkpoint tells the second
  machine to point `CATALOG_URI` at the same shared catalog. Without S3 the second machine must
  ingest independently from the public bulk-data host. That is *more* work to set up and a
  *stronger* proof — it demonstrates the ingest is deterministic too, not just the backtest — but
  plan 01-09 Task 3's instructions would need rewriting to say so.

**The real cost of trimming**, stated honestly rather than minimised: Phase 2 needs real history.
`DATA-04` (point-in-time universe), `DATA-05` (data quality monitors on real gaps and outliers) and
`SIM-05` all read multi-symbol multi-year data, and Phase 2's headline gate is reproducing a
published backtest — which cannot be done over one month of one symbol. The ingest is not avoided
by trimming, only moved, and moving it means Phase 2 opens with an infrastructure task instead of
the correctness work it is scoped for. Against that: doing it now front-loads AWS credentials,
egress and a cache design into a phase whose whole argument is thinness, and every hour spent there
is an hour the spine is not yet proven end to end.

### How to take it

If the developer chooses TRIM: update D-21, D-22 and D-23 in `01-CONTEXT.md` to record the
deferral with this reasoning, drop Task 2 from this plan (leaving Task 1 and its requirement IDs
intact), remove the `aws-s3` entry from this plan's `user_setup`, rewrite plan 01-09 Task 3's
second-machine instructions to independent ingest, and add a data-expansion plan to the phase that
needs it. If the developer chooses to keep the current scope, no action is needed — the plan below
is already that.
</scope_option>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-05-SUMMARY.md
@engine/data/binance_vision.py
@engine/data/catalog.py
@engine/money.py
</context>

<flagged_assumptions>
The `DATA-01`, `DATA-02` and `DATA-06` probe rows are `unclassified` and remain unresolved; they
are surfaced as flagged planner assumptions in plan 01-05, which first implements each of them,
and are not double-counted here.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The timestamp-unit boundary, close-time semantics, and resumable verified ingest (DATA-01, DATA-06, D-25)</name>
  <files>engine/data/binance_vision.py, engine/tests/data/test_binance_vision.py, engine/tests/data/fixtures/klines_ms_era.csv, engine/tests/data/fixtures/klines_us_era.csv, engine/tests/data/fixtures/klines_corrupt.zip, engine/tests/data/fixtures/klines_corrupt.zip.CHECKSUM</files>
  <read_first>
    - engine/data/binance_vision.py (the tracer's loader from plan 01-05 — this task hardens it)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md (`### Pitfall 1` in full, including the two verified sample timestamp values, and `### Pattern 3`)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md (D-24, D-25)
    - engine/money.py (the decimal parsing every price and quantity goes through)
  </read_first>
  <behavior>
    - Test 1: `detect_timestamp_unit` returns milliseconds for a 13-digit value and microseconds for a 16-digit value, using the two verified samples from RESEARCH.md as the literal test inputs.
    - Test 2: parsing the millisecond-era fixture and the microsecond-era fixture both yield bars whose dates fall in the expected calendar year — the microsecond fixture must not produce a year beyond 2100.
    - Test 3: a file whose rows are internally consistent at one unit and a second file at the other unit, ingested into one range, produce a single strictly increasing timestamp sequence with no backward step and no jump larger than the bar interval times a tolerance.
    - Test 4: every produced bar's event timestamp equals its source row's close-time field, for both eras.
    - Test 5: the event timestamp never equals the source row's open-time field for any bar — the assertion that the wrong convention was not silently chosen.
    - Test 6: a corrupted archive whose checksum does not match raises `ChecksumMismatch`, the archive is not parsed, and the process exits non-zero.
    - Test 7: after a `ChecksumMismatch`, no partially-extracted file remains on disk and nothing was written to the catalog.
    - Test 8: re-running ingestion over an already-ingested range performs no download for the verified files and produces no new catalog rows.
    - Test 9: interrupting ingestion partway and re-running resumes from the first unverified file rather than restarting.
    - Test 10: an archive entry whose path escapes the extraction directory is rejected without being written.
  </behavior>
  <action>
Extract the unit detection into a named `detect_timestamp_unit(raw_value)` that returns a
nanosecond multiplier from the digit count of the raw timestamp string — 13 digits are
milliseconds, 16 are microseconds. Detect it **per file**, not once per ingest and not from the
date in the filename: RESEARCH.md verified by live fetch that the two eras are byte-identical in
CSV shape, with no header change, no version marker and no filename change, so the digit count is
the only self-describing signal. A file published late as a historical backfill, or one spanning a
timezone boundary, is covered by digit-count detection and is not covered by a filename heuristic.
Raise on any digit count that is neither 13 nor 16, rather than guessing — an unexpected width is
a new upstream change and should stop the ingest loudly the way the last one should have.

Check the detected unit's consistency within a file and raise if rows disagree, so a mixed file
surfaces immediately rather than producing a sequence with one impossible jump in it.

Keep the close-time convention from D-24 and add the negative assertion: no bar's event timestamp
may equal its row's open-time value. The positive assertion alone passes for a symbol whose open
and close happen to be tested at a boundary where both look plausible; the negative one is what
catches a silent convention flip.

Add monthly-archive support alongside the daily archives the tracer used. Full available history
per symbol from its listing date (D-22) is thousands of daily files and dozens of monthly ones;
prefer monthly for the bulk and fall back to daily for the current partial month. Verify the
checksum sidecar for every archive of either kind.

Add `IngestLedger`: a small on-disk record of which archives have been downloaded and
checksum-verified, so re-running skips them. D-25 requires idempotent and resumable ingestion, and
a full-history multi-symbol ingest is long enough that it will be interrupted at least once. The
ledger records the archive name and its verified digest; a file is skipped only when both its
presence and its recorded digest match, so a truncated download is re-fetched rather than trusted.

Harden the extraction path: read entries by explicit member name into a controlled directory and
reject any entry whose resolved path escapes it. Never call a bulk extract-all on an archive
fetched over the network. On a checksum mismatch, remove any partially written output before
raising, so a subsequent run does not find and trust a half-file.

Check in four fixtures: a small millisecond-era CSV, a small microsecond-era CSV, a deliberately
corrupted archive, and its published-digest sidecar. Every parsing behavior above must be testable
with no network access, so the suite stays fast and stays green on a machine with no egress.
  </action>
  <verify>
    <automated>uv run --project engine pytest engine/tests/data/test_binance_vision.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 10 passed</fails_when>
    <automated>grep -cE '^\s*(TIMESTAMP_UNIT|UNIT_NS|_UNIT)\s*=\s*[0-9_]+' engine/data/binance_vision.py</automated>
    <fails_when>prints anything other than 0 — a module-level constant fixing the timestamp unit is exactly the bug Pitfall 1 describes</fails_when>
    <automated>grep -c 'extractall' engine/data/binance_vision.py</automated>
    <fails_when>prints anything other than 0</fails_when>
    <automated>grep -cE '\bfloat\(' engine/data/binance_vision.py</automated>
    <fails_when>prints anything other than 0</fails_when>
    <automated>head -1 engine/tests/data/fixtures/klines_us_era.csv | cut -d, -f1 | tr -d '\n' | wc -c | grep -qx 16</automated>
    <fails_when>non-zero exit, meaning the microsecond-era fixture does not actually carry 16-digit timestamps and therefore proves nothing</fails_when>
  </verify>
  <acceptance_criteria>
    - `detect_timestamp_unit` exists as a named export and is called per file, not once per ingest.
    - `engine/data/binance_vision.py` declares no module-level constant fixing the timestamp unit.
    - Both era fixtures are checked in, and the microsecond fixture's first field is exactly 16 characters wide.
    - A test asserts the event timestamp equals the close-time field AND does not equal the open-time field, for both eras.
    - A checksum mismatch raises `ChecksumMismatch`, leaves no partial file, and writes nothing to the catalog — asserted by passing tests.
    - Re-running a completed ingest downloads nothing and adds no catalog rows — asserted by a passing test.
    - The module contains no `extractall` call and no `float(` call.
  </acceptance_criteria>
  <done>An ingest spanning the 2025-01-01 unit change produces one correct, strictly increasing bar sequence; corrupt data hard-fails and leaves nothing behind; a re-run is a no-op.</done>
</task>

<task type="auto">
  <name>Task 2: Full-history multi-symbol ingest into the shared object-store catalog (DATA-02, D-21, D-22, D-23)</name>
  <precondition>`CATALOG_URI` points at an existing object-storage bucket path and the running identity can write to it.</precondition>
  <reversibility rating="reversible">D-23 — the catalog is a cache re-derivable from the public bulk-data host; moving it back to local disk is a URI change.</reversibility>
  <files>engine/data/catalog.py, engine/cli/ingest.py, engine/tests/data/test_catalog.py</files>
  <read_first>
    - engine/data/catalog.py (the tracer's local catalog from plan 01-05)
    - engine/data/binance_vision.py (hardened in Task 1 — the loader this drives)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md (D-21, D-22, D-23 including the planner note about local iteration speed)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md (`## Standard Stack` supporting table — the storage-options row, and Assumptions Log row A1 which flags the credential-key format as unverified)
  </read_first>
  <action>
Move the catalog behind `catalog_uri_from_env()`, which reads `CATALOG_URI` and returns either a
local path or an object-store URI. The catalog constructor accepts storage options for object-store
credentials. RESEARCH.md's assumption A1 flags the exact credential-key format as unverified — it
may want filesystem-abstraction-style keys or it may want the cloud SDK's environment variables.
Resolve this by trying the documented form first and asserting a real round trip (write one bar,
read it back) before ingesting anything; if the documented form does not work, record the working
form in the plan SUMMARY so the next reader does not repeat the investigation.

Add a local read-through cache in front of the object store, keyed on the catalog path, so
repeated local backtests do not pay an object-store round trip per read. CONTEXT.md's own planner
note anticipates this: D-23's shared-bytes property is what makes success criterion 2's two-machine
claim testable, but paying network latency on every iteration would make the loop unpleasant enough
that someone quietly switches back to local-only. The cache directory is gitignored by plan 01-01.

Create `engine/cli/ingest.py` taking `--symbol` (repeatable), `--interval`, `--from` and `--to`,
all as parameters with no defaults baked into the module. D-21 is explicit that the loader is
written generally and that breadth arrives when Phase 3 needs it; the parameterisation is what makes
that true, and the assumption-delta decision recorded in plan 01-05 requires no symbol string
constant anywhere in `engine/`.

Ingest 1-hour bars over the full available history per symbol from its listing date, per D-22.
Choose four symbols: BTCUSDT, ETHUSDT, BNBUSDT and one listed materially later — RESEARCH.md's
open question 3 recommends a 2021-or-later listing specifically so at least one genuinely different
range boundary goes through the loader now rather than being assumed correct until Phase 2. Record
the four chosen symbols and each one's discovered listing date in the plan SUMMARY.

Do not attempt the survivorship-free universe. CONTEXT.md defers it explicitly and the roadmap
names it as the hard part of ingestion; Phase 1 takes a handful of majors and Phase 2 owns the
delisted-symbol work. Do not ingest 1-minute bars either — the loader is parameterised for the
interval and D-22 defers the actual ingest until live execution resolution matters.

Write the instrument definitions into the catalog alongside the bars, sourced from the exchange's
public instrument loader so the real per-symbol maker and taker rates are present. A catalog with
bars and no instruments backtests only until the venue needs a precision filter it does not have.
  </action>
  <verify>
    <automated>uv run --project engine pytest engine/tests/data/test_catalog.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed"</fails_when>
    <automated>uv run --project engine ingest --symbol BTCUSDT --symbol ETHUSDT --symbol BNBUSDT --interval 1h --from 2017-08-17 --to 2025-06-01</automated>
    <fails_when>non-zero exit, or stderr contains "ChecksumMismatch", or stdout does not report a per-symbol bar count greater than zero for all three symbols</fails_when>
    <automated>uv run --project engine ingest --symbol BTCUSDT --interval 1h --from 2017-08-17 --to 2025-06-01</automated>
    <fails_when>non-zero exit, or the run reports a download count greater than zero for a range already fully ingested and verified</fails_when>
    <automated>uv run --project engine python -c "import engine.data.catalog as c; print(c.catalog_uri_from_env())"</automated>
    <fails_when>non-zero exit, or the printed value is a bare relative path rather than the configured catalog location</fails_when>
    <automated>grep -rn 'BTCUSDT' engine/ --include='*.py' | grep -v '^engine/tests/' | wc -l | grep -qx 0</automated>
    <fails_when>non-zero exit, meaning a symbol string was hardcoded into a non-test engine module</fails_when>
  </verify>
  <acceptance_criteria>
    - `engine/data/catalog.py` exports `catalog_uri_from_env` and the catalog location comes from the environment, never from a literal in the module.
    - A write-then-read round trip against the configured object store succeeds before any bulk ingest begins.
    - `engine/cli/ingest.py` accepts a repeatable `--symbol` plus `--interval`, `--from` and `--to`, with no symbol, interval or date default baked into the module.
    - Four symbols are ingested at 1-hour resolution over their full available history, and the SUMMARY records each symbol with its discovered listing date.
    - At least one ingested symbol has a listing date in 2021 or later, so a materially different range boundary was exercised.
    - A second identical ingest run downloads nothing.
    - Instrument definitions are present in the catalog alongside the bars.
    - No non-test module under `engine/` contains the literal `BTCUSDT`.
    - No 1-minute bars and no delisted-symbol universe work were attempted.
  </acceptance_criteria>
  <done>Four symbols of full 1-hour history, checksum-verified across the timestamp-unit boundary, live in a catalog that CI and both machines read, and a re-run is free.</done>
</task>

</tasks>

<!-- planner-discipline-allow: BTCUSDT -->

<review_dispositions>
Findings from `01-REVIEWS.md` (cross-AI review, 2026-09-06) that landed on this plan.

| Finding | Reviewer | Disposition |
|---|---|---|
| **The phase is too thick for its stated tracer-first purpose** (`01-07-PLAN.md:227`) — full history, four symbols, object storage, read-through caching, resumable ledgers and live AWS round trips are not needed to demonstrate one deterministic BTCUSDT backtest, and this contradicts CONTEXT.md's "breadth arrives when Phase 3 needs it" | Codex (MEDIUM) | **Escalated, not decided.** The finding is accepted as accurate — D-21's own text does say that, and this plan does go well past it. But cutting this plan changes what Phase 1 delivers and defers three binding CONTEXT.md decisions (D-21, D-22, D-23), which is not a planner's call to make unilaterally. Recorded as `<scope_option>` above with the full cost on both sides: exactly what stays, exactly what moves, that **no requirement ID leaves Phase 1** (all 14 stay — `DATA-02` is already satisfied by the tracer's catalog and the shared-bytes property comes from D-23, not from the requirement), that all six success criteria remain reachable with criterion 2 changing shape, and that the real price of trimming is Phase 2 opening with an infrastructure task. Nothing was deleted; the choice is made cheap for a human to take either way. |
| OpenCode: evaluate simpler storage (local disk with a clear upgrade path to S3) instead of S3 in this plan | OpenCode (LOW, uncited) | **Subsumed by the scope option, and noted as the cheaper half of it.** Option TRIM's storage half is exactly this suggestion, and it is already nearly free structurally: `catalog_uri_from_env()` exists precisely so the catalog location is a URI rather than a code path, so "local disk with an upgrade path" is the same code with a different environment variable. If the developer wants only the storage half of TRIM without the symbol/history half, that is a coherent third option — it defers D-23 alone and leaves D-21 and D-22 intact. |
| Codex: defer S3 read-through caching and four-symbol full-history ingestion unless essential to the acceptance test | Codex (suggestion) | **Same escalation.** Assessed against the acceptance test rather than by preference: the acceptance test in plan 01-05 is one symbol over one month, and plan 01-09's two-machine checkpoint uses the same range, so neither needs this plan's Task 2. What does need it is Phase 2. That trade-off is stated in the scope option rather than settled here. |
</review_dispositions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public bulk-data host → local filesystem | thousands of archives are downloaded and extracted |
| local process → object storage | catalog writes cross the network with cloud credentials |
| object storage → CI and both developer machines | the same bytes are read by three independent consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-26 | Tampering | archive extraction over a full-history ingest | high | mitigate | Entries are read by explicit member name into a controlled directory with an escape check; the module contains no bulk extract-all call, asserted by an acceptance criterion. Scale is what makes this matter — one bad entry in thousands of archives is easy to miss |
| T-01-27 | Tampering | resumable ingest ledger | high | mitigate | A file is skipped only when both its presence and its recorded verified digest match, so a truncated or replaced download is re-fetched rather than trusted on filename alone |
| T-01-28 | Tampering | silently misdated bars across the unit boundary | high | mitigate | Per-file digit-count detection, an intra-file consistency check, and a raise on any unexpected digit width; the negative assertion that no bar carries its open-time value catches a silent convention flip |
| T-01-29 | Information Disclosure | object-store credentials | medium | mitigate | Credentials come from the environment or the ambient cloud identity, never from a committed file; the catalog holds only public market data, so the exposure ceiling is write access rather than data secrecy |
| T-01-30 | Tampering | shared catalog writable by CI | medium | mitigate | CI is granted read-only on the bucket; only the developer identity writes. A CI job that could rewrite the catalog could silently change what every determinism test compares against |
| T-01-31 | Denial of Service | full-history download volume | low | accept | Four symbols of 1-hour bars is a bounded, one-time cost against a public host designed for bulk download; the ingest ledger means an interrupted run is never repeated from scratch |
</threat_model>

<verification>
- Both era fixtures parse to correctly dated, close-timestamped bars.
- A corrupt archive hard-fails and leaves nothing.
- A second identical ingest downloads nothing.
- Four symbols with materially different listing dates are in the shared catalog with their instruments.
</verification>

<success_criteria>
- `DATA-01`: checksum-verified ingestion that hard-fails on mismatch and resumes on re-run.
- `DATA-02`: a catalog the engine reads natively, shared across machines and CI.
- `DATA-06`: close-time semantics asserted on both sides of the timestamp-unit boundary.
</success_criteria>

<artifacts_this_phase_produces>
| Kind | Symbol / path |
|---|---|
| function | `engine/data/binance_vision.py`: `detect_timestamp_unit()` |
| class | `engine/data/binance_vision.py`: `IngestLedger` |
| function | `engine/data/catalog.py`: `catalog_uri_from_env()` |
| module | `engine/cli/ingest.py`: `main()` |
| console script | `ingest` |
| CLI flags | `--symbol` (repeatable), `--interval`, `--from`, `--to` |
| env var | `CATALOG_URI`, `AWS_REGION` |
| fixtures | `engine/tests/data/fixtures/klines_ms_era.csv`, `klines_us_era.csv`, `klines_corrupt.zip`, `klines_corrupt.zip.CHECKSUM` |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-07-SUMMARY.md` when done
</output>
