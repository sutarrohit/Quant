---
phase: 01-walking-skeleton-backtest-a-hand-written-spec-on-real-data
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - schemas/package.json
  - schemas/strategy-spec.v1.json
  - schemas/README.md
  - schemas/scripts/generate.mjs
  - schemas/guards/money-guard.stub.test.ts
  - schemas/guards/money-guard.stub.sql
  - engine/money.py
  - engine/dsl/spec.py
  - engine/tests/test_money_guard.py
  - engine/tests/dsl/test_spec_contract.py
  - docs/adr/0003-decimal-string-codegen.md
autonomous: false
requirements: [FOUND-04, FOUND-09]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`schemas/strategy-spec.v1.json` is the single source of truth; the Pydantic model and the TypeScript types are both generated from it at build time into gitignored directories, so neither language can drift into authority (D-06, FOUND-04)"
    - "A money field declared in the schema emits a TypeScript `string`, never a `number` — the JS float representation D-07 forbids is unreachable from the generated types"
    - "A raw `float` passed to a monetary field raises a Pydantic validation error naming the decimal type, so a float reaching a money field fails the build (FOUND-09)"
    - "Two spec files whose canonical JSON bytes are equal produce the same `params_hash`; any field difference produces a different hash"
    - "A spec file that is empty, `{}`, or `null` is rejected by schema validation with a non-zero exit, never treated as a default spec"
    - "Key order in the spec JSON does not affect validation or `params_hash` — the canonical form sorts keys before hashing"
    - "A money string with exactly the schema's maximum declared scale of 8 decimal places validates; a string with 9 decimal places is rejected by the pattern rather than silently truncated"
    - "`Decimal('1.50')` and `Decimal('1.5')` compare equal as money, and both serialize to the one canonical decimal-string form used by `params_hash`"
    - "A money field that is an empty string, `null`, or absent raises a validation error and is never coerced to `Decimal('0')`"
    - "Money quantization sets `ROUND_HALF_EVEN` explicitly rather than relying on the ambient decimal context, and no `float()` call appears on any money path"
    - statement: "Money fields serialize with sorted keys and a fixed decimal-string format everywhere they enter a hash input, so field insertion order cannot change a recorded hash"
      verification: backstop
  artifacts:
    - path: "schemas/strategy-spec.v1.json"
      provides: "the versioned StrategySpec contract, bounded to exactly what the SMA crossover spec of D-26 needs"
      contains: "$id"
      min_lines: 60
    - path: "schemas/scripts/generate.mjs"
      provides: "the two-generator codegen step producing Pydantic and TypeScript into gitignored output dirs"
    - path: "engine/money.py"
      provides: "the strict-Decimal money boundary and the canonical decimal-string serializer"
      exports: ["Money", "to_canonical_str", "parse_money", "MONEY_SCALE"]
      min_lines: 40
    - path: "engine/dsl/spec.py"
      provides: "StrategySpec load + validate + params_hash, importing only generated models and stdlib"
      exports: ["StrategySpec", "load_spec", "params_hash"]
    - path: "engine/tests/test_money_guard.py"
      provides: "the FOUND-09 build-failing guard for Python"
    - path: "schemas/guards/money-guard.stub.test.ts"
      provides: "D-07 TypeScript guard stub — skipped, made real in Phase 4"
    - path: "schemas/guards/money-guard.stub.sql"
      provides: "D-07 SQL guard stub — NUMERIC-or-TEXT assertion, made real in Phase 4"
    - path: "docs/adr/0003-decimal-string-codegen.md"
      provides: "the recorded decision on how a string-typed JSON Schema money field becomes a strict Python Decimal"
  key_links:
    - from: "engine/dsl/spec.py"
      to: "schemas/strategy-spec.v1.json"
      via: "imports the generated Pydantic model produced from the schema; never hand-declares spec fields"
      pattern: "from engine.generated"
    - from: "schemas/scripts/generate.mjs"
      to: "schemas/strategy-spec.v1.json"
      via: "both generators read the schema as their only input"
      pattern: "strategy-spec\\.v1\\.json"
  prohibitions:
    - statement: "MUST NOT widen a tolerance, round a value, or relax a decimal comparison in order to make a money assertion pass — a failing money assertion is a real defect in a system whose entire trust argument rests on exact decimal arithmetic, never a test that needs loosening"
      category: values
---

<objective>
Make `schemas/strategy-spec.v1.json` the single source of truth for the strategy contract, wire
the two-language codegen pipeline behind it, and build the Python half of the float-in-money guard.

Purpose: `FOUND-04` says one schema, three languages, no drift; `FOUND-09` says a float reaching a
money field fails the build. RESEARCH.md found a real gap between those two: the documented
decimal path in the Python generator keys off `"type": "number"`, which is exactly the JSON wire
type D-07 forbids the TypeScript side from seeing. That gap is an open question, not a known
answer, and this plan opens with a spike whose output is a recorded decision rather than an
assumed mechanism.
Output: the versioned schema, a working generate step, a strict-Decimal money boundary, a guard
test that fails on a float, and TS/SQL guard stubs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-01-SUMMARY.md
@engine/pyproject.toml
</context>

<tasks>

<task type="checkpoint:decision">
  <name>Task 1: Spike then decide — how a string-typed JSON Schema money field becomes a strict Python Decimal</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md (`### Pitfall 3`, `## Open Questions` item 1, and `## Assumptions Log` row A3)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md (D-06 and D-07)
  </read_first>
  <context>
Before presenting options, run the spike — do not present this decision from reasoning alone.
RESEARCH.md's assumption A3 explicitly says to confirm against the tool's own `--help` before
committing to a mechanism, and the recommendation is to inspect real generated output rather than
predict it.

1. Read the Python generator's `--help` and grep it for every flag mentioning decimal, strict
   types, custom templates, and custom base classes. A3 is that a documented flag for the
   `"type": "string"` case may exist and was missed; if it does, the decision is already made.
2. Write a throwaway three-field repro schema in a scratch directory: one plain string, one money
   field declared `{"type": "string", "pattern": "^-?[0-9]+(\\.[0-9]{1,8})?$"}`, one integer.
3. Generate a Pydantic v2 model from it and read the emitted file. Record verbatim what type the
   money field received.
4. Generate TypeScript from the same schema and record verbatim what type the money field received.

The evidence to bring to this decision is those two emitted type declarations, quoted.
  </context>
  <decision>
Which mechanism converts the generated money field into a strict `Decimal` on the Python side,
while the TypeScript side keeps emitting `string`?
  </decision>
  <options>
**Option A — a hand-written wrapper module outside the generated file.** `engine/money.py` owns a
`parse_money()` that calls `Decimal(value)` on the known money-field names after generation.
  - Pro: the generated file stays untouched and regenerable; no generator internals depended on.
  - Pro: the conversion is one small, readable, testable file — the lazy option that works.
  - Con: the money-field name list is maintained by hand and can drift from the schema; needs a
    test that walks the schema and asserts every money-patterned field is in the list.

**Option B — a custom template directory passed to the generator.** The generator emits a
`field_validator` on the money fields directly.
  - Pro: no hand-maintained field list — the schema drives it end to end.
  - Con: couples the build to the generator's internal template structure, which is not a stable
    public contract; a generator upgrade can silently stop applying the validator.

**Option C — a custom base class the generated models inherit.** The base class carries the strict
config and a model validator that coerces pattern-matched string fields to `Decimal`.
  - Pro: uses a documented generator flag rather than template internals.
  - Pro: the strict config lives in one place and applies to every future generated model.
  - Con: the coercion rule has to identify money fields at runtime rather than from the schema.

**Recommendation if the spike shows no documented flag:** Option A, with the schema-walking drift
test that closes its single weakness. It is the smallest mechanism that satisfies both D-06 (schema
is authoritative) and D-07 (Python gets Decimal, TypeScript gets string), and it depends on no
generator internal. Only prefer B or C if the spike shows the generated output already carries a
usable hook.
  </options>
  <resume-signal>
Select: `a` (wrapper module), `b` (custom template dir), `c` (custom base class), or `flag:&lt;name&gt;`
if the spike found a documented generator flag that maps a pattern-constrained string field
directly to `Decimal` — a real flag wins over all three options. Do not answer before the spike's
two generated type declarations have been quoted into ADR-0003.
  </resume-signal>
  <acceptance_criteria>
    - The spike's emitted Python money-field type and TypeScript money-field type are both quoted verbatim in `docs/adr/0003-decimal-string-codegen.md`.
    - The ADR records the chosen mechanism and the rejected alternatives with one line each.
    - The ADR states whether a documented generator flag for the string-to-Decimal case exists, closing RESEARCH.md assumption A3 either way.
  </acceptance_criteria>
  <done>The mechanism is chosen on the evidence of real generated output and written into ADR-0003; Tasks 2 and 3 implement the choice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the versioned schema and wire the two-generator codegen step (FOUND-04, D-06)</name>
  <reversibility rating="costly">D-06 — reversing to a Python-first model means rewriting the schema by hand and re-deriving the TypeScript, and any spec already persisted was validated against the generated shape.</reversibility>
  <files>schemas/package.json, schemas/strategy-spec.v1.json, schemas/README.md, schemas/scripts/generate.mjs, engine/dsl/spec.py, engine/tests/dsl/test_spec_contract.py</files>
  <read_first>
    - docs/adr/0003-decimal-string-codegen.md (written in Task 1 — the chosen mechanism)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md (D-06, D-26 — the DSL surface is bounded to exactly what an SMA crossover needs)
    - .gitignore (confirm `engine/generated/` and `schemas/generated/` are already ignored by plan 01-01)
    - packages/fastapi-server/package.json (the workspace package-script conventions to match)
  </read_first>
  <behavior>
    - Test 1: the schema validates the hand-written SMA crossover spec.
    - Test 2: an empty file, `{}`, and `null` each fail validation with a non-zero exit and a message naming the missing required key — none is treated as a default spec.
    - Test 3: `params_hash` of a spec is unchanged when its top-level keys are reordered.
    - Test 4: `params_hash` differs when any single field value changes, including a change from `"5"` to `"5.0"` in a non-money integer field.
    - Test 5: two spec files with byte-different formatting but equal parsed content hash identically.
    - Test 6: a `fast_period` greater than or equal to `slow_period` is rejected by schema constraint, since a crossover between a window and itself has no defined semantics.
    - Test 7: the generated TypeScript declaration for the money-typed field is `string`, asserted by reading the generated `.d.ts`.
  </behavior>
  <action>
Create `schemas/` as a workspace package `@repo/schemas` (private) with `json-schema-to-typescript`
as its only dependency and scripts `"build": "node scripts/generate.mjs"` and
`"check-types": "node scripts/generate.mjs --check"`. The workspace already lists `schemas` — plan
01-01 added the entry against this directory.

Write `schemas/strategy-spec.v1.json` as a draft 2020-12 JSON Schema with an `$id` of
`https://quant-platform.local/schemas/strategy-spec.v1.json` and a required `spec_version` field
whose only permitted value is `"1"`. Bound the surface to exactly what D-26's SMA crossover needs
and no further: the phase proves the seam, not the expressiveness, and CONTEXT.md records the
developer considering a richer strategy and deliberately reverting. That means: a `symbol` string,
a `bar_interval` string, an `indicators` object declaring `fast_period` and `slow_period` as
positive integers with `fast_period` constrained below `slow_period`, an `entry` rule of the single
permitted form (fast crosses above slow), an `exit` rule of the single permitted form (fast
crosses back below slow), and a `sizing` object with one money-shaped `trade_size`. Set
`additionalProperties: false` at every level so an unrecognised key is a validation error rather
than silently ignored data.

Declare every money-shaped field as `{"type": "string", "pattern": "^-?[0-9]+(\\.[0-9]{1,8})?$"}`.
This is the deliberate choice from RESEARCH.md's Pitfall 3: declaring money as a JSON number with a
multiple-of constraint would generate a constrained decimal on the Python side but emit a plain
TypeScript `number`, which is precisely the JS float representation D-07 forbids. The pattern's
maximum scale of 8 decimal places is the boundary Test 7's sibling assertions check.

Write `schemas/scripts/generate.mjs` running both generators against that one schema: TypeScript
into `schemas/generated/ts/strategy-spec.d.ts`, and the Pydantic v2 model into
`engine/generated/strategy_spec.py` via the engine's own uv environment. Both output directories
are gitignored by plan 01-01, so no generated file is ever a committed artifact. The `--check`
mode regenerates into a temporary directory and diffs, so CI fails when the committed schema and
the generated output have diverged. Add an `__init__.py` write step for `engine/generated/` so the
generated module is importable.

Write `engine/dsl/spec.py` exposing `StrategySpec` (a thin re-export of the generated model),
`load_spec(path)` which reads, JSON-parses, and validates — raising a named error rather than
returning `None` on any of empty file, `{}`, or `null` — and `params_hash(spec)` returning a
SHA-256 hex digest over the canonical serialization: sorted keys, no whitespace, money values as
their canonical decimal strings via `engine.money`. This module imports only the generated model,
`engine.money`, and the standard library. It must never import the trading engine, and no comment
or docstring anywhere under `engine/dsl/` may name that module, because the purity gate is a bare
recursive content grep over this directory and a mention in prose makes the gate report a
violation that does not exist. State the rule in `schemas/README.md` and `engine/README.md`, both
of which sit outside the grepped path.
  </action>
  <verify>
    <automated>pnpm --filter @repo/schemas run build</automated>
    <fails_when>non-zero exit, or stderr contains "Cannot find module"</fails_when>
    <automated>test -f engine/generated/strategy_spec.py &amp;&amp; test -f schemas/generated/ts/strategy-spec.d.ts</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>grep -cE 'trade_size\??: *number' schemas/generated/ts/strategy-spec.d.ts</automated>
    <fails_when>prints anything other than 0 — a numeric money type in the generated TypeScript is the exact D-07 violation this schema shape exists to prevent</fails_when>
    <automated>uv run --project engine pytest engine/tests/dsl/test_spec_contract.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 7 passed</fails_when>
    <automated>git status --porcelain engine/generated schemas/generated</automated>
    <fails_when>prints any line, meaning a generated artifact is untracked-but-visible to git and therefore not correctly ignored</fails_when>
    <automated>grep -rl nautilus_trader engine/dsl/ ; test $? -eq 1</automated>
    <fails_when>non-zero exit from the wrapper, meaning grep matched at least one file under engine/dsl/</fails_when>
  </verify>
  <acceptance_criteria>
    - `schemas/strategy-spec.v1.json` declares `$id`, `spec_version` with const `"1"`, and `additionalProperties: false` at every object level.
    - Every money-shaped field in the schema has `"type": "string"` and a decimal pattern; no money field is declared as `"type": "number"`.
    - `pnpm --filter @repo/schemas run build` produces both generated files.
    - The generated TypeScript declaration for `trade_size` is `string`.
    - `engine/dsl/spec.py` exports `StrategySpec`, `load_spec`, `params_hash`.
    - `engine/dsl/spec.py` imports nothing beyond the generated module, `engine.money`, and the standard library.
    - `grep -rl nautilus_trader engine/dsl/` prints nothing.
    - No file under `engine/generated/` or `schemas/generated/` appears in `git status`.
  </acceptance_criteria>
  <done>One schema drives both languages, the generated TypeScript money type is a string, spec loading rejects every empty-shaped input loudly, and params_hash is key-order independent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The float-in-money guard — Python real, TypeScript and SQL as stubs (FOUND-09, D-07)</name>
  <files>engine/money.py, engine/tests/test_money_guard.py, schemas/guards/money-guard.stub.test.ts, schemas/guards/money-guard.stub.sql</files>
  <read_first>
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md (`### Money-guard test (Python) — the FOUND-09 enforcement mechanism` and `### Pitfall 2`)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md (D-07 — three languages, only Python buildable this phase)
    - schemas/strategy-spec.v1.json (written in Task 2 — the decimal pattern and its scale)
    - engine/tests/conftest.py (fixture conventions)
  </read_first>
  <behavior>
    - Test 1: constructing the strict money model with a raw `float` raises a validation error whose message names the decimal type.
    - Test 2: constructing it with a `Decimal` instance succeeds.
    - Test 3: constructing it with a decimal string succeeds — this is the wire format D-07 mandates.
    - Test 4 (boundary): a money string with exactly 8 decimal places validates; a string with 9 decimal places raises, rather than being silently truncated to 8.
    - Test 5 (adjacency): `parse_money("1.50")` and `parse_money("1.5")` compare equal, and `to_canonical_str` maps both to one identical string.
    - Test 6 (empty): `""`, `None`, and an absent field each raise a validation error; none produces `Decimal("0")`.
    - Test 7 (precision): quantizing a half-way value uses banker's rounding — `2.5` at zero scale gives `2`, and `3.5` gives `4` — proving the rounding mode is explicitly set rather than inherited from an ambient context another module could mutate.
    - Test 8 (leak): walking a nested dict/list structure for any `float` value finds none in a structure built from `to_canonical_str` output; the walker is the reusable assertion the run-artifact tests reuse in plan 01-09.
  </behavior>
  <action>
Create `engine/money.py` exposing `MONEY_SCALE = 8` matching the schema pattern's maximum scale,
a strict Pydantic model `Money` with `model_config = ConfigDict(strict=True)` and a single
`Decimal` field, `parse_money(value)` returning a `Decimal` from a string or `Decimal` and raising
for anything else, `to_canonical_str(value)` producing the one canonical decimal-string form used
by every hash input, and `assert_no_floats(obj)` — a recursive walker that raises naming the path
where a `float` was found.

Set the rounding mode explicitly with `Decimal.quantize(..., rounding=ROUND_HALF_EVEN)` at every
quantization site. Do not rely on the ambient decimal context: it is process-global mutable state,
so a value's rounding would depend on whatever else ran first in the process, which makes success
criterion 2's byte-identical claim conditional on import order.

Never call `float()` on a money path. Guard against the specific leak RESEARCH.md's Pitfall 2
found: one engine position field is a raw float while every other money-shaped field the engine
emits is a decimal string, and the engine's own report generator deletes that column before
returning. Any persistence path in this project goes through the engine's report generators for
the same reason; `assert_no_floats` is what makes a regression there loud. Note this in the
docstring of `assert_no_floats` by describing the field's role rather than pasting the engine's
internal field name, so the walker's own source cannot become the thing a leak-detection grep
trips on.

Write `engine/tests/test_money_guard.py` covering all eight behaviors above. This is the test that
fails the build under `FOUND-09`, so it runs in the default suite and is not marked skip or xfail.

Write the two stubs D-07 requires. `schemas/guards/money-guard.stub.test.ts` is a skipped
TypeScript test asserting that a money value crossing the wire is `string` or `bigint` and never
`number`, with a header comment saying it becomes real in Phase 4 when TypeScript first handles
money. `schemas/guards/money-guard.stub.sql` is a commented query asserting that every monetary
column in the schema has type `numeric` or `text` and never `double precision`/`real`, written
against `information_schema.columns` and ready to be turned on in Phase 4. Both stubs are
deliberately inert: D-07 says only the Python guard is buildable in Phase 1, and a stub that
silently passes as if it were real would be worse than an honest stub. Neither stub is wired into
any CI job by this plan.
  </action>
  <verify>
    <automated>uv run --project engine pytest engine/tests/test_money_guard.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 8 passed</fails_when>
    <automated>grep -cE '\bfloat\(' engine/money.py</automated>
    <fails_when>prints anything other than 0</fails_when>
    <automated>grep -c 'ROUND_HALF_EVEN' engine/money.py</automated>
    <fails_when>prints 0</fails_when>
    <automated>test -f schemas/guards/money-guard.stub.test.ts &amp;&amp; test -f schemas/guards/money-guard.stub.sql</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>uv run --project engine ruff check engine/money.py engine/dsl</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `engine/money.py` exports `Money`, `parse_money`, `to_canonical_str`, `assert_no_floats`, `MONEY_SCALE`.
    - `MONEY_SCALE` equals 8 and matches the schema pattern's maximum decimal places.
    - `engine/money.py` contains zero occurrences of `float(`.
    - `engine/money.py` contains at least one `ROUND_HALF_EVEN`.
    - `pytest engine/tests/test_money_guard.py` reports 8 or more passing tests and none skipped.
    - Both guard stubs exist and are not referenced by any CI workflow or turbo task in this plan.
  </acceptance_criteria>
  <done>A float reaching a money field fails a test that runs in the default suite; the TypeScript and SQL guards exist as honest, inert stubs awaiting Phase 4.</done>
</task>

</tasks>

<!-- planner-discipline-allow: nautilus_trader -->
<!-- planner-discipline-allow: trade_size\??: *number -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec file → engine | a hand-written or later AI-authored JSON file crosses into the evaluator; this is the first untrusted-input boundary in the system |
| schema → generated code | generated Python and TypeScript are executed; the generator is a build-time code-execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Tampering | `schemas/strategy-spec.v1.json` input validation | high | mitigate | `additionalProperties: false` at every level plus required-field and const `spec_version` constraints; `load_spec` raises on empty/`{}`/`null` rather than defaulting. ASVS V5 for this phase is shape validation only — the semantic validator with rejection messages is DSL-02 in Phase 2 |
| T-01-12 | Tampering | float reaching a monetary field | high | mitigate | Strict Pydantic `Decimal` guard plus `assert_no_floats` walker, both exercised by a non-skipped test in the default suite (FOUND-09) |
| T-01-13 | Spoofing | generated code passed off as the contract | medium | mitigate | Generated directories are gitignored (D-06), so a hand-edited generated model cannot be committed and mistaken for the source of truth; `--check` mode diffs regenerated output in CI |
| T-01-14 | Information Disclosure | spec file paths supplied on the CLI | low | accept | Phase 1 has a single local operator and no multi-user surface; path handling hardens in Phase 4 when the control plane accepts uploaded specs |
</threat_model>

<verification>
- `pnpm --filter @repo/schemas run build` regenerates both languages from the one schema.
- The generated TypeScript money type is `string`.
- `pytest engine/tests/test_money_guard.py engine/tests/dsl/test_spec_contract.py` is green.
- `grep -rl nautilus_trader engine/dsl/` prints nothing.
</verification>

<success_criteria>
- `FOUND-04`: one versioned schema, two generated languages, neither authoritative.
- `FOUND-09`: a float reaching a money field fails the build in Python; TypeScript and SQL guards exist as declared stubs per D-07.
</success_criteria>

<artifacts_this_phase_produces>
| Kind | Symbol / path |
|---|---|
| JSON Schema | `schemas/strategy-spec.v1.json` — `$id` `https://quant-platform.local/schemas/strategy-spec.v1.json` |
| schema fields | `spec_version` (const `"1"`), `symbol`, `bar_interval`, `indicators.fast_period`, `indicators.slow_period`, `entry`, `exit`, `sizing.trade_size` |
| npm package | `@repo/schemas` — scripts `build`, `check-types` |
| file | `schemas/scripts/generate.mjs` (supports `--check`) |
| generated (gitignored, never an artifact) | `engine/generated/strategy_spec.py`, `schemas/generated/ts/strategy-spec.d.ts` |
| module | `engine/money.py`: `Money`, `parse_money()`, `to_canonical_str()`, `assert_no_floats()`, `MONEY_SCALE = 8` |
| module | `engine/dsl/spec.py`: `StrategySpec`, `load_spec()`, `params_hash()` |
| stub | `schemas/guards/money-guard.stub.test.ts`, `schemas/guards/money-guard.stub.sql` |
| doc | `docs/adr/0003-decimal-string-codegen.md` |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-04-SUMMARY.md` when done
</output>
