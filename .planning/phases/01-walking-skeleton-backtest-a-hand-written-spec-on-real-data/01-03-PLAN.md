---
phase: 01-walking-skeleton-backtest-a-hand-written-spec-on-real-data
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .gitmodules
  - vendor/nautilus_trader
  - engine/pyproject.toml
  - engine/uv.lock
  - .devcontainer/devcontainer.json
  - .devcontainer/Dockerfile
  - docs/adr/0001-shelve-tenancy-patch.md
  - docs/adr/0002-rust-rebuild-time-tripwire.md
  - engine/tests/test_engine_import.py
autonomous: false
requirements: [FOUND-01, FOUND-03]
user_setup:
  - service: rust-toolchain
    why: "D-04 installs the trading engine editable from source, which compiles a large Rust workspace; a wheel would never exercise the rebuild time PROJECT.md tripwire 3 requires measuring"
    env_vars:
      - name: CARGO_HOME
        source: "rustup default location; only needs setting if the developer relocates the cargo cache"
    dashboard_config:
      - task: "Install rustup and let it honour the submodule's rust-toolchain.toml (channel 1.98.0)"
        location: "https://rustup.rs"
  - service: github
    why: "FOUND-03 preserves the shelved tenancy branch as an annotated tag on the fork"
    env_vars: []
    dashboard_config:
      - task: "Push access to the nautilus_trader fork used as the submodule origin, so the archive tag can be pushed"
        location: "github.com — the fork remote configured on the existing local checkout"

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The trading engine is consumed as a git submodule pinned to an exact commit, not a published wheel, and no local patch is applied to it (FOUND-01)"
    - "A stock v2 live node can be *constructed* from the pinned commit through the v2 builder entry point — proving the pin yields a usable node type, not merely an importable package. Starting a node against live infrastructure is out of Phase 1 scope and belongs to Phase 5 (PAPER-01)"
    - "The archive tag exists on the fork *remote*, verified with `ls-remote`, not only in the local checkout — a local-only tag dies with the machine and would not satisfy D-05's preservation claim"
    - "`engine/pyproject.toml` declares a `snapshot` dependency group that excludes the editable engine path dependency, so the daily cron of plan 01-06 can install its dependencies from the same lockfile without compiling Rust"
    - "`git -C vendor/nautilus_trader status --porcelain` is empty after a fresh checkout and build — the vendored source is stock"
    - "A developer with a clean clone can reach a working engine import with a documented, reproducible command sequence"
    - "The cold Rust build time is measured, recorded as a number in an ADR, and dated — so PROJECT.md tripwire 3 has a baseline rather than an assumption"
    - "The shelved tenancy branch is preserved as a named tag and its defect evidence is written down with file:line citations (FOUND-03)"
    - "The devcontainer pins Python 3.13, Node, pnpm and the same Rust channel the submodule pins, so success criterion 2's two-machine claim is an environment guarantee rather than a hope (D-10)"
  artifacts:
    - path: ".gitmodules"
      provides: "the submodule declaration for the vendored engine"
      contains: "vendor/nautilus_trader"
    - path: "docs/adr/0001-shelve-tenancy-patch.md"
      provides: "FOUND-03 written record — which seams were valuable, why the branch was shelved, and where it is preserved"
      min_lines: 40
    - path: "docs/adr/0002-rust-rebuild-time-tripwire.md"
      provides: "the measured cold and warm rebuild times with the machine spec and date"
      min_lines: 25
    - path: ".devcontainer/devcontainer.json"
      provides: "pinned Python 3.13, Node, pnpm and Rust 1.98.0 development environment"
    - path: "engine/tests/test_engine_import.py"
      provides: "proof the editable install resolves, the pinned version is the expected one, and a stock v2 live node constructs from that pin"
  key_links:
    - from: "engine/pyproject.toml"
      to: "vendor/nautilus_trader"
      via: "editable path dependency on the submodule's python subdirectory"
      pattern: "vendor/nautilus_trader"
    - from: ".devcontainer/devcontainer.json"
      to: "vendor/nautilus_trader/rust-toolchain.toml"
      via: "the container's Rust channel matches the channel the submodule pins"
      pattern: "1\\.98\\.0"
  prohibitions: []
---

<objective>
Vendor NautilusTrader v2 as a commit-pinned git submodule installed editable, measure the cold
Rust rebuild time and record the number, pin the development environment, and write the tenancy
archive record.

Purpose: `FOUND-01` requires a stock engine at a pinned upstream commit with no local patches, and
D-04 chose a submodule over a wheel precisely so that PROJECT.md's tripwire 3 — "how long does a
Rust rebuild take" — becomes measurable instead of theoretical. RESEARCH.md's Pitfall 4 is blunt
about the sequencing risk: any plan that puts adapter work on the critical path without first
measuring the build is guessing about its own iteration speed. This plan takes the measurement
before the tracer is written.
Output: `vendor/nautilus_trader` at a pinned commit, an editable install that imports, a recorded
rebuild-time baseline, a pinned devcontainer, and two ADRs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-01-SUMMARY.md
@engine/pyproject.toml
</context>

<flagged_assumptions>
Spec-less probe fallback — unresolved (`unclassified`) probe rows this plan owns:

- **FOUND-01** — the probe returned `unclassified`; no edge category was derived. Planner
  assumption, flagged for the developer: the only edge that matters here is *pin drift* (does the
  build still resolve if upstream moves), and it is authored as the `git status --porcelain` and
  pinned-commit truths above rather than as a probe-derived edge.
- **FOUND-03** — the probe returned `unclassified`, which is correct: this is a documentation
  requirement with no data-shape axis at all. No edge is authored; the requirement is covered by
  the ADR artifact criteria.
</flagged_assumptions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy gate — the pinned engine commit and the Rust build toolchain</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md (`## Package Legitimacy Audit` and the `## Standard Stack` rows for the engine and maturin)
    - .planning/PROJECT.md (Key Decisions — the engine decision and its four revisit tripwires)
  </read_first>
  <what-built>Nothing yet — this gate runs BEFORE the submodule is added and BEFORE the first Rust build. The engine is the trust anchor of the entire platform, so a human confirms the build backend and the exact commit pin before either executes code on this machine.</what-built>
  <how-to-verify>
Two items need a human look before the first build, and this checkpoint is never auto-approvable.

**1. `maturin` (PyPI).** Flagged `[SUS]` for `too-new` + `unknown-downloads`, the same checker
artifact as the plan 01-01 list. Open https://pypi.org/project/maturin and confirm the project is
the long-established PyO3 build backend, that version `1.15.0` exists, and that the repo link is
the PyO3 organisation. The version is not a guess — the submodule's own
`python/pyproject.toml` build-system pins `maturin==1.15.0` exactly.

**2. The engine commit pin.** The submodule is pinned to
`be9eaff8a7f50ec72dd54cd0cd894fdc127e69bf`. Confirm:
  - the origin remote is the intended fork of `nautechsystems/nautilus_trader`;
  - that commit's `version.json` reports `v2.0.0rc4`;
  - `rust-toolchain.toml` at that commit pins channel `1.98.0` (already verified at plan time);
  - the commit is on a branch that tracks upstream, so the pin is a point on upstream history and
    not a divergent local edit — `FOUND-01` requires stock, unpatched source.

**Approve** if both hold.
**Reject** if the maturin page does not resolve to the PyO3 project, or if the pinned commit
carries local modifications relative to upstream — a patched engine violates the requirement this
whole plan exists to satisfy.

> **GATE OUTCOME (recorded at execution, 2026-09-06):** maturin cleared as written — PyO3-org
> `maturin==1.15.0` confirmed. The original pin `be9eaff8a7f50ec72dd54cd0cd894fdc127e69bf` was
> **REJECTED**: it is the fork's `multi-tenant-nautilus-runtime` HEAD, a merge sitting on top of
> tenancy patch `768cbf3664`, and `crates/common/src/tenant.rs` exists at that commit — a patched
> engine, violating FOUND-01 and D-04. It is not an ancestor of `origin/develop`.
> The human named the replacement pin **`4692bac35bb11a25eeebb8d7af4d51c55afe53ec`**
> ("Pin docs.rs checks to compatible nightly"), verified at execution time to have
> `crates/common/src/tenant.rs` absent, `version.json` reporting `v2.0.0rc4`,
> `rust-toolchain.toml` pinning `1.98.0`, and to be an ancestor of both `origin/develop` and
> `origin/master` — the last commit on the fork branch before the tenancy patch landed. Every
> literal SHA reference below this point in the plan has been updated to the corrected pin. See
> `01-03-SUMMARY.md` for the full deviation record.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Task 2, or name which of the two items failed to halt the plan.</resume-signal>
  <acceptance_criteria>
    - A human has recorded approve/reject for the maturin package and for the commit pin.
    - On reject, no submodule is added and the plan halts naming the failing item.
  </acceptance_criteria>
  <done>Human has approved or rejected; the decision and the confirmed commit SHA are recorded in the plan SUMMARY. RESOLVED: maturin approved; original commit pin rejected (carried tenancy patch); corrected pin `4692bac35bb11a25eeebb8d7af4d51c55afe53ec` approved by human.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor the engine as a pinned submodule, install editable, and pin the dev environment (D-04, D-10, FOUND-01)</name>
  <precondition>`rustup show` reports an installed toolchain and `cargo --version` succeeds on PATH.</precondition>
  <reversibility rating="reversible">D-04 — swapping the editable submodule for a published wheel later is a lockfile change; the source paths PROJECT.md cites simply stop being readable in-tree.</reversibility>
  <files>.gitmodules, vendor/nautilus_trader, engine/pyproject.toml, engine/uv.lock, .devcontainer/devcontainer.json, .devcontainer/Dockerfile, engine/tests/test_engine_import.py</files>
  <read_first>
    - engine/pyproject.toml (written by plan 01-01 — the dependency block this task extends)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md (`## Standard Stack` installation block, and the `maturin`/`uv`/Rust rows with their verified version pins)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md (D-04 and D-10 in full, including the CI cost consequence)
  </read_first>
  <action>
Add the fork as a git submodule at `vendor/nautilus_trader` and check it out at exactly
`4692bac35bb11a25eeebb8d7af4d51c55afe53ec` (corrected pin — see Task 1 gate outcome above; the
plan's original pin `be9eaff8a7f50ec72dd54cd0cd894fdc127e69bf` carried the tenancy patch and was
rejected). Pin the commit, not the `v2.0.0rc4` tag — a tag can be
moved, and `FOUND-01` says "pinned upstream commit". A local checkout of the same fork already
exists on this machine outside the repository tree; do not copy from it and do not add it as a
path reference. Clone fresh into `vendor/` so the submodule's recorded URL is the fork remote and
a clean clone of this repository reproduces the same bytes.

Apply no patch of any kind to anything under `vendor/`. The whole point of D-04's reversal of the
tenancy fork is that the vendored source stays stock; a local edit there silently invalidates
`FOUND-01` and every argument PROJECT.md's Key Decisions table makes.

Extend `engine/pyproject.toml` with an editable path dependency on `vendor/nautilus_trader/python`
using uv's path-dependency mechanism (a `[tool.uv.sources]` entry marking the package editable,
paired with the dependency name in `[project.dependencies]`). Add `maturin==1.15.0` to the dev
group — the exact pin comes from the submodule's own build-system requirement, not from a
preference. Re-lock and commit `engine/uv.lock`.

In the same edit, declare a `snapshot` dependency group under `[dependency-groups]` listing exactly
what the daily `exchangeInfo` cron needs and nothing else: the HTTP client, `sqlalchemy` and
`psycopg[binary]`. This task is the right owner because this task is what makes the group
necessary — before the editable path dependency exists there is nothing heavy to exclude. Plan
01-06's scheduled workflow installs it with
`uv sync --frozen --no-install-project --only-group snapshot`, which resolves from this same
committed lockfile (so the group cannot drift the way a hand-maintained package list in a workflow
file would) while `--no-install-project` keeps the editable path dependency — and therefore the
whole Rust build — out of a job that runs every day. Verify here rather than in 01-06 that the
group installs cleanly and that the trading engine is absent from the resulting environment; the
cost of discovering otherwise on the cron is a silently missed day of a non-backfillable history.

Read `MIGRATION_V2.md` in the vendored checkout before writing any code against the engine. It is
required reading per CONTEXT.md: v1-era tutorials and most model training data describe classes
that do not exist in v2, and the live node builder replaced the v1 trading node entirely. Plan
01-05 depends on this having been read.

Write `.devcontainer/devcontainer.json` and its `Dockerfile` pinning, per D-10: Python 3.13, Node
(matching the root `engines.node` floor), pnpm 10.34.5, uv in the `>=0.12,<0.13` range the
submodule's own `required-version` demands, and the Rust channel `1.98.0` that
`vendor/nautilus_trader/rust-toolchain.toml` pins. Do not pin a different Rust version "to be
safe": the toolchain file is authoritative and a mismatch between the container and the submodule
is precisely the two-machine divergence success criterion 2 forbids. Mount a persistent volume for
the cargo registry and the `target/` directory so a container rebuild does not force a cold Rust
compile — the vendored workspace's build directory reaches double-digit gigabytes.

Write `engine/tests/test_engine_import.py` with two assertions. First, that the engine package
imports and reports version string `2.0.0rc4`, so a silently re-resolved dependency fails a test
rather than surfacing as a mysterious behaviour change three plans later.

Second — `test_live_node_builder_constructs` — that the v2 live node **builder entry point exists
and yields a node object** from this pinned commit. Take the exact builder signature from
`MIGRATION_V2.md` and the vendored source rather than from memory: v1's trading node class does not
exist in v2 and most training data describes it anyway. Configure the smallest node the builder
accepts: no data clients, no execution clients, no credentials, no network. Assert the returned
object is a node instance; do **not** start it, do not run its event loop, and do not attempt a
connection.

An import test alone proves the pin resolves; it does not prove the pin yields a usable node type,
which is what FOUND-01's wording claims. This construction test is the cheapest thing that closes
that gap without dragging live infrastructure into Phase 1.

If minimal construction turns out to require live infrastructure — a running message bus, a
credential, an event loop already spinning — do not fake it and do not weaken the assertion into a
tautology. Mark the test `pytest.mark.skip` with a reason string naming the exact infrastructure it
demanded, and record in the plan SUMMARY that FOUND-01's Phase 1 scope is therefore **construction
attempted, startup deferred to Phase 5 (PAPER-01)**. Flag in the SUMMARY that
`REQUIREMENTS.md` FOUND-01 ("a stock NautilusTrader v2 `LiveNode` runs from a pinned upstream
commit") then claims more than Phase 1 delivers, and that a human should decide whether to amend
the requirement text or move it. Do not leave the requirement silently overstated either way.
  </action>
  <verify>
    <automated>git submodule status vendor/nautilus_trader</automated>
    <fails_when>non-zero exit, or the output line begins with "-" (submodule not initialised) or "+" (checked-out commit differs from the recorded pin)</fails_when>
    <automated>test "$(git -C vendor/nautilus_trader rev-parse HEAD)" = "4692bac35bb11a25eeebb8d7af4d51c55afe53ec"</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>test -z "$(git -C vendor/nautilus_trader status --porcelain)"</automated>
    <fails_when>non-zero exit, meaning the vendored source has local modifications and is no longer stock</fails_when>
    <automated>uv run --project engine pytest engine/tests/test_engine_import.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed", or the summary line reports 2 skipped (both assertions cannot be unavailable — the version assertion has no infrastructure dependency)</fails_when>
    <automated>grep -c 'live_node_builder' engine/tests/test_engine_import.py</automated>
    <fails_when>prints 0, meaning the FOUND-01 node-construction assertion was never written</fails_when>
    <automated>grep -v '^//' .devcontainer/devcontainer.json | grep -c '1.98.0'</automated>
    <fails_when>prints 0</fails_when>
    <automated>uv sync --project engine --frozen --no-install-project --only-group snapshot &amp;&amp; uv run --project engine --no-sync python -c "import importlib.util,sys; sys.exit(0 if importlib.util.find_spec('nautilus_trader') is None else 1)"</automated>
    <fails_when>non-zero exit — either the snapshot group does not resolve from the committed lockfile, or the trading engine is importable in an environment that is supposed to exclude it, which means the daily cron would pay a Rust build</fails_when>
  </verify>
  <acceptance_criteria>
    - `.gitmodules` declares one submodule at path `vendor/nautilus_trader` pointing at the fork URL.
    - `git -C vendor/nautilus_trader rev-parse HEAD` equals `4692bac35bb11a25eeebb8d7af4d51c55afe53ec` (corrected pin).
    - `git -C vendor/nautilus_trader status --porcelain` produces no output.
    - `engine/pyproject.toml` references the path `vendor/nautilus_trader/python` and declares `maturin==1.15.0` in the dev group.
    - `engine/pyproject.toml` declares a `snapshot` dependency group containing only the HTTP client, `sqlalchemy` and `psycopg[binary]`, and that group installs from the committed lockfile without the trading engine present.
    - `engine/tests/test_engine_import.py` asserts the version string equals `2.0.0rc4` and contains a `test_live_node_builder_constructs`.
    - The node-construction test either passes, or is skipped with a reason string naming the live infrastructure it required — and in the skip case the SUMMARY records the FOUND-01 scope narrowing and flags the requirement wording for a human.
    - `.devcontainer/devcontainer.json` names Python `3.13`, pnpm `10.34.5`, and Rust `1.98.0`.
    - `engine/uv.lock` is committed and reflects the path dependency.
  </acceptance_criteria>
  <done>A clean clone plus `git submodule update --init` plus `uv sync --project engine` yields an importable, version-asserted engine at the pinned commit from which a stock v2 node constructs; the devcontainer pins the same toolchain the submodule does; and the engine-free snapshot group installs from the same lockfile.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the Rust rebuild time and write both ADRs (PROJECT.md tripwire 3, FOUND-03, D-05)</name>
  <files>docs/adr/0002-rust-rebuild-time-tripwire.md, docs/adr/0001-shelve-tenancy-patch.md</files>
  <read_first>
    - .planning/PROJECT.md (the Key Decisions table — the engine decision, its four revisit tripwires, and the tenancy reversal with its full defect evidence and file:line citations)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md (D-05 for the ADR filename and the archive tag name)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md (`### Pitfall 4: Rust rebuild time is unmeasured and gates the whole engine workstream`)
    - vendor/nautilus_trader/Makefile (the build target the measurement times)
  </read_first>
  <action>
Take three timed measurements against the vendored workspace and record the actual numbers:

1. **Cold build** — with the workspace's build directory removed and the cargo registry cache
   present, time the submodule's debug build target end to end.
2. **Warm no-op rebuild** — run the same target again immediately, changing nothing.
3. **Warm one-touch rebuild** — update the modification time of one Rust source file in the crate
   the strategy routing lives in, then rebuild.

Measurement 3 must not modify file **contents**. Use `touch` on
`vendor/nautilus_trader/crates/trading/src/strategy/mod.rs`, which advances the mtime cargo's
fingerprinting keys off while leaving the bytes identical — so `git status --porcelain` inside the
submodule stays empty and the stock-source invariant this plan exists to establish is never
transiently violated. Do not append a character and delete it afterwards: between the two steps the
submodule is dirty, and any failure in the rebuild leaves it that way.

If cargo turns out to fingerprint on content hashes rather than mtimes and the touch produces a
no-op identical to measurement 2, fall back to an explicit reversible edit, in this exact order:
make the edit, run the timed rebuild, restore with
`git -C vendor/nautilus_trader checkout -- <path>`, then assert
`git -C vendor/nautilus_trader status --porcelain` is empty and record in the ADR that the fallback
was used. The assertion comes after the restoration, never before it, and the ADR says which of the
two methods produced the number so the next measurement is comparable.

These three numbers are what PROJECT.md's tripwire 3 needs: the first says what a CI cold image
costs, the second says what the build system's overhead floor is, and the third says what the
actual edit loop feels like — which is the number that decides whether a Python-side change can be
iterated on quickly or whether every touch pays a compile.

Write `docs/adr/0002-rust-rebuild-time-tripwire.md` with the three numbers, the date, the machine
(CPU model, core count, RAM), the toolchain version, whether the cargo cache was warm, and the
exact command used for each. A measurement with no machine spec is not reproducible and cannot be
compared against the next one. State plainly whether the measured edit loop crosses the threshold
PROJECT.md's tripwire sets, and if it does, say so as a finding rather than softening it — the
tripwire exists to trigger a revisit, and a number that is quietly rounded down defeats it.

Write `docs/adr/0001-shelve-tenancy-patch.md` per D-05, carrying the defect evidence and file:line
citations that already exist in PROJECT.md's Key Decisions table rather than re-deriving them.
The ADR must state: which seams in the tenancy work were genuinely valuable and worth revisiting;
the specific defects that made the patch untenable, each with its `file:line` citation; the
decision to run one live node process per tenant so that isolation is a property of the process
boundary instead of a patch; and where the branch is preserved. Preserve it as an annotated tag
`archive/tenancy-768cbf3664` on the fork that backs the submodule, and record in the ADR the exact
command that restores it. `FOUND-03` is satisfied by the written record, not by the tag alone — a
tag with no explanation is a branch nobody will ever dare delete and nobody will ever read.

**Push the tag and verify it on the remote.** Create it locally, then
`git -C vendor/nautilus_trader push origin refs/tags/archive/tenancy-768cbf3664`, then confirm with
`ls-remote` that the ref actually exists on the fork. D-05's claim is preservation *on the fork*,
and a tag that exists only in this checkout is preserved exactly as long as this machine survives —
which is not preservation, it is a note to self. The verify command below checks the remote ref,
not the local one. If the push is rejected for lack of permission, halt and surface it rather than
recording the ADR as done: the ADR would then be documenting a preservation that has not happened.

Create the `docs/adr/` directory as part of this task; it does not exist yet, and these two files
establish the numbering convention for every later ADR in the project.
  </action>
  <verify>
    <automated>test -f docs/adr/0001-shelve-tenancy-patch.md &amp;&amp; test -f docs/adr/0002-rust-rebuild-time-tripwire.md</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>grep -cE '[0-9]+(\.[0-9]+)?\s*(s|sec|seconds|m|min|minutes)' docs/adr/0002-rust-rebuild-time-tripwire.md</automated>
    <fails_when>prints a value below 3, meaning fewer than three timing figures were recorded</fails_when>
    <automated>grep -cE '\.rs:[0-9]+|\.py:[0-9]+' docs/adr/0001-shelve-tenancy-patch.md</automated>
    <fails_when>prints 0, meaning the ADR carries no file:line defect citation</fails_when>
    <automated>git -C vendor/nautilus_trader ls-remote --tags origin 'refs/tags/archive/tenancy-768cbf3664'</automated>
    <fails_when>non-zero exit, or prints nothing — the archive tag is absent from the fork remote, so D-05's preservation claim is unbacked no matter what the local checkout holds</fails_when>
    <automated>test -z "$(git -C vendor/nautilus_trader status --porcelain)"</automated>
    <fails_when>non-zero exit, meaning the rebuild measurement left the vendored source dirty and FOUND-01's stock-source invariant is broken</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/adr/0002-rust-rebuild-time-tripwire.md` records three distinct measured durations, each with the command that produced it, plus CPU model, core count, RAM, toolchain version, and date.
    - The ADR states explicitly whether the measured edit loop crosses PROJECT.md tripwire 3's threshold.
    - The ADR names which method produced measurement 3 — mtime touch, or the reversible-edit fallback — so the next measurement is comparable.
    - `git -C vendor/nautilus_trader status --porcelain` is empty **after** the measurements, asserted last, with any fallback edit already restored.
    - `docs/adr/0001-shelve-tenancy-patch.md` contains at least one `file:line` citation and names the seams worth revisiting.
    - The ADR names the restore command for the `archive/tenancy-768cbf3664` tag.
    - `git -C vendor/nautilus_trader ls-remote --tags origin 'refs/tags/archive/tenancy-768cbf3664'` prints a ref line — the tag is on the fork, not merely local.
  </acceptance_criteria>
  <done>The rebuild-time baseline exists as three dated numbers with machine context, the vendored source is still stock afterwards, and the shelved tenancy work is preserved on the fork remote with a written record carrying its defect evidence.</done>
</task>

</tasks>

<review_dispositions>
Findings from `01-REVIEWS.md` (cross-AI review, 2026-09-06) that landed on this plan.

| Finding | Reviewer | Disposition |
|---|---|---|
| **FOUND-01 is not actually satisfied** — the plan verifies only that the package imports and reports `2.0.0rc4`, which proves pinning, not that a stock v2 node starts | Codex (HIGH) | **Accepted, resolved honestly rather than papered over.** Verified: the previous Task 2 wrote only an import-plus-version assertion. Added `test_live_node_builder_constructs`, which builds the smallest node the v2 builder accepts — no clients, no credentials, no network — and asserts the returned object is a node. It deliberately does **not** start the node: startup needs live infrastructure and belongs to Phase 5 (PAPER-01). The plan now states that narrowing explicitly, and if even construction turns out to need infrastructure the task requires a skip **with a named reason** plus a SUMMARY entry flagging that `REQUIREMENTS.md` FOUND-01's wording then claims more than Phase 1 delivers, for a human to amend or move. The requirement is not left silently overstated in either branch. |
| **The archive-tag check verifies only a local tag** (`01-03-PLAN.md:275`) while D-05 claims preservation on the fork | Codex (MEDIUM) | **Accepted, fixed.** Task 3 now pushes the tag and the verify command is `ls-remote --tags origin`, which fails when the ref is absent from the fork. A rejected push halts the task rather than letting the ADR document a preservation that did not happen. |
| **Touching a Rust file for the rebuild measurement violates the clean-submodule invariant unless restoration is explicit** (`01-03-PLAN.md:240`) | Codex (MEDIUM) | **Accepted, fixed — and the fix removes the dirty window entirely rather than managing it.** Measurement 3 now uses `touch` on `crates/trading/src/strategy/mod.rs`: cargo fingerprints on mtime, so the rebuild is triggered with zero content change and the submodule is never dirty at any point. The reversible-edit path survives only as a documented fallback, with the order pinned as edit → measure → `git checkout --` restore → assert clean, and the ADR must name which method produced the number. A cleanliness assertion was also added to Task 3's verify block, since the previous one lived only in Task 2 and could not see what Task 3 did. |
| **Snapshot dependency isolation is not designed concretely** (`01-06-PLAN.md:274`) — no uv dependency-group or packaging mechanism named; an explicit package list drifts from the lockfile, syncing the project compiles Rust | Codex (MEDIUM) | **Accepted, fixed here** rather than in 01-06, because this is the task that introduces the editable path dependency and therefore creates the problem. `engine/pyproject.toml` now declares a `[dependency-groups]` `snapshot` group, installed by `uv sync --frozen --no-install-project --only-group snapshot`: `--frozen` keeps it tied to the committed lockfile so it cannot drift like a hand-written workflow package list, `--no-install-project` keeps the editable engine — and the Rust build — out. The verify command proves both halves here, where a failure costs a plan iteration, rather than on the cron, where it costs a permanently missing day. |
| OpenCode: add a verification step for the Nautilus submodule URL validity before adding the submodule | OpenCode (LOW, uncited) | **Rejected as already covered twice over.** Task 1 is a blocking human gate that requires confirming the origin remote is the intended fork and that the pinned commit is a point on upstream history. Beyond that, `git submodule add` against an unreachable URL fails loudly on its own — a pre-check would only restate the failure that is about to happen anyway. |
| OpenCode: the plans do not handle the Nautilus fork becoming unavailable | OpenCode (LOW, uncited) | **Deferred, deliberately.** A vendored submodule at a pinned SHA is already the mitigation for upstream disappearing — the source is in every clone. Building a second mirror in Phase 1 would be infrastructure for a failure mode the vendoring decision (D-04) exists to survive. Revisit if the fork remote is ever actually lost. |
</review_dispositions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream git remote → local build | submodule source is compiled and executed on every developer and CI machine |
| PyPI → build environment | the build backend runs arbitrary code at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC2 | Tampering | maturin install, submodule source | high | mitigate | Blocking human legitimacy checkpoint (Task 1) covers the build backend and the commit pin before any build runs; the exact-commit pin means an upstream force-push cannot silently change what compiles |
| T-01-08 | Tampering | vendored source drift | high | mitigate | `git -C vendor/nautilus_trader status --porcelain` must be empty — a required, machine-checked acceptance criterion, so a local patch fails the plan rather than quietly satisfying FOUND-01 on paper |
| T-01-09 | Elevation of Privilege | compiled Rust extension loaded into the Python process | medium | accept | The engine is the deliberate trust anchor of the whole system (PROJECT.md Key Decisions); the mitigation is the exact-commit pin and the stock-source assertion above, not sandboxing a library the platform is built on |
| T-01-10 | Denial of Service | cold Rust build in CI | medium | mitigate | Devcontainer mounts a persistent cargo registry and build-directory volume; the measured cold-build number recorded in ADR-0002 is what plan 01-09's CI caching strategy is sized against |
</threat_model>

<verification>
- The submodule is initialised at the pinned SHA with a clean working tree, asserted again after the rebuild measurements.
- The engine imports, reports the pinned version, and a stock v2 node constructs from it.
- The `snapshot` dependency group installs from the committed lockfile with the trading engine absent.
- Three rebuild timings exist as numbers in a dated ADR, with the method for measurement 3 named.
- The archive tag resolves **on the fork remote** and its restore command is documented.
</verification>

<success_criteria>
- `FOUND-01` as scoped for Phase 1: stock engine, pinned commit, no local patches, and a node that constructs — machine-asserted, not asserted in prose. Node *startup* is explicitly Phase 5 scope, and the narrowing is recorded rather than assumed.
- `FOUND-03`: the tenancy branch is archived on the fork with a written record carrying its defect evidence.
- PROJECT.md tripwire 3 has a measured baseline before any adapter work begins.
</success_criteria>

<artifacts_this_phase_produces>
| Kind | Symbol / path |
|---|---|
| submodule | `vendor/nautilus_trader` @ `4692bac35bb11a25eeebb8d7af4d51c55afe53ec` (v2.0.0rc4, corrected pin — original `be9eaff8a7f` rejected at Task 1 gate for carrying the tenancy patch) |
| file | `.gitmodules` |
| dependency | editable path dependency on `vendor/nautilus_trader/python` in `engine/pyproject.toml` |
| dependency | `maturin==1.15.0` (engine dev group) |
| dependency group | `[dependency-groups]` `snapshot` in `engine/pyproject.toml` — HTTP client, `sqlalchemy`, `psycopg[binary]`; installed by plan 01-06 with `uv sync --frozen --no-install-project --only-group snapshot` |
| file | `.devcontainer/devcontainer.json`, `.devcontainer/Dockerfile` — Python 3.13, pnpm 10.34.5, uv >=0.12,<0.13, Rust 1.98.0 |
| test | `engine/tests/test_engine_import.py` — asserts engine version `2.0.0rc4`; `test_live_node_builder_constructs` asserts a stock v2 node constructs from the pin |
| doc | `docs/adr/0001-shelve-tenancy-patch.md` |
| doc | `docs/adr/0002-rust-rebuild-time-tripwire.md` |
| git tag | `archive/tenancy-768cbf3664` on the submodule origin fork |
| directory | `docs/adr/` (new ADR numbering convention for the project) |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-03-SUMMARY.md` when done
</output>
