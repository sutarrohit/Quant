---
phase: 01-walking-skeleton-backtest-a-hand-written-spec-on-real-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - .gitignore
  - engine/pyproject.toml
  - engine/package.json
  - engine/uv.lock
  - engine/README.md
  - engine/dsl/__init__.py
  - engine/adapters/__init__.py
  - engine/data/__init__.py
  - engine/cli/__init__.py
  - engine/persistence/__init__.py
  - engine/tests/__init__.py
  - engine/tests/conftest.py
  - engine/tests/dsl/__init__.py
  - engine/tests/dsl/test_scaffold_smoke.py
  - packages/fastapi-server/pyproject.toml
  - packages/fastapi-server/package.json
  - packages/fastapi-server/README.md
autonomous: false
requirements: [FOUND-05, FOUND-06]
user_setup:
  - service: local-toolchain
    why: "uv 0.12.x and pnpm 10.34.5 must be on PATH before the engine project resolves"
    env_vars: []
    dashboard_config:
      - task: "Confirm `uv --version` reports 0.12.x (>=0.12,<0.13 per the Nautilus submodule's own required-version pin)"
        location: "local shell"

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`engine/dsl/` exists as a real Python package directory at the repository root, so success criterion 3's literal grep path resolves"
    - "`pnpm-workspace.yaml` no longer contains an `apps/*` entry, and `pnpm install` completes without resolving apps/web or apps/server"
    - "`uv run --project engine pytest` executes and reports at least one collected test"
    - "`packages/fastapi-server` no longer declares fastapi-blog domain dependencies (aiosqlite, alembic, pwdlib, pyjwt)"
    - "Generated model directories are gitignored, so no generated Pydantic or TypeScript file can ever be committed as a plan artifact"
  artifacts:
    - path: "engine/pyproject.toml"
      provides: "uv project definition for the engine, ruff/pytest toolchain config matching the repo convention"
      contains: "[tool.pytest.ini_options]"
      min_lines: 30
    - path: "engine/package.json"
      provides: "turbo shim delegating test/lint/check-types/build to uv"
      contains: "uv run pytest"
    - path: "engine/tests/conftest.py"
      provides: "shared synthetic-bar fixtures for evaluator unit tests, independent of any ingested data"
      min_lines: 20
    - path: "pnpm-workspace.yaml"
      provides: "workspace membership: packages/*, engine, prisma, schemas — and no apps/*"
    - path: ".gitignore"
      provides: "ignore rules for generated model dirs, run outputs, local catalog, plot PNGs"
  key_links:
    - from: "package.json"
      to: "engine/package.json"
      via: "turbo run test/lint/check-types resolves the engine workspace member and invokes its uv-delegating scripts"
      pattern: "turbo run (test|lint|check-types)"
    - from: "pnpm-workspace.yaml"
      to: "engine/package.json"
      via: "workspace packages list includes the engine directory"
      pattern: "^\\s*-\\s*[\"']?engine"
  prohibitions: []
---

<objective>
Restructure the Turborepo template into the architecture tree from commit one (D-01), and stand
up `engine/` as a uv project on Python 3.13 with a working pytest harness (D-02).

Purpose: every other plan in this phase writes into `engine/`. Nothing can be built, tested, or
grep-enforced until this tree exists and the test runner runs. This plan also lands the
`engine/dsl/` path that success criterion 3's literal grep is anchored to — per D-02 that path is
a published acceptance criterion, not an implementation detail.
Output: a workspace whose members are `packages/*`, `engine`, `prisma`, `schemas`; an `engine/`
uv project with ruff + pytest configured; a green (if trivial) test run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-PATTERNS.md
@packages/fastapi-server/pyproject.toml
@packages/fastapi-server/package.json
@pnpm-workspace.yaml
@turbo.json
@package.json
</context>

<flagged_assumptions>
Spec-less probe fallback: the deterministic edge probe classified FOUND-05 and FOUND-06 edges,
which are carried by plan 01-08 (the evaluator/purity plan that owns the semantics). This plan
carries none of the 26 probe edges; it establishes the paths those edges are asserted against.
</flagged_assumptions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — verify every third-party package before first install</name>
  <read_first>
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md (the `## Package Legitimacy Audit` table and the note beneath it)
  </read_first>
  <what-built>Nothing yet — this gate runs BEFORE the first install. RESEARCH.md's Package Legitimacy Audit flagged twelve third-party packages `[SUS]`; the protocol requires a human to look at each before any of them executes install-time code on this machine.</what-built>
  <how-to-verify>
Every package below was flagged `[SUS]` by the legitimacy checker for the single reason
`too-new` (plus `unknown-downloads` for the PyPI entries). RESEARCH.md argues this is a checker
artifact — the "published" date is each package's most recent release, not its founding date —
but the protocol requires a human to look before the first install, and this checkpoint is never
auto-approvable regardless of `workflow.auto_advance`.

Open each page and confirm the package is the well-known project it claims to be (repo link
resolves, release history spans years, maintainer is the expected org):

PyPI — https://pypi.org/project/&lt;name&gt;
  - pydantic
  - sqlalchemy
  - psycopg
  - pyarrow
  - polars
  - matplotlib
  - datamodel-code-generator
  - pytest
  - ruff

npm — https://www.npmjs.com/package/&lt;name&gt;
  - prisma
  - @prisma/client
  - json-schema-to-typescript

Two packages are deliberately NOT in this list and are gated separately in plan 01-03:
`maturin` and the NautilusTrader source (a pinned git submodule, never a PyPI install, per D-04).
`import-linter` is the one entry with a genuinely thin signal (no repo URL returned) and is
deliberately NOT installed — RESEARCH.md's "Don't Hand-Roll" table rejects it in favour of the
literal grep that success criterion 3 already specifies.

**Approve** if every page resolves to the expected long-established project.
**Reject** and stop the phase if any page 404s, shows a first release within the last 90 days,
or points at a repo that is not the canonical one.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Task 2, or name the package that failed verification to halt the phase.</resume-signal>
  <acceptance_criteria>
    - A human has recorded approve/reject for all twelve packages listed above.
    - On reject, no install task runs and the plan halts with the rejected package named.
  </acceptance_criteria>
  <done>Human has approved or rejected the package list; the decision is recorded in the plan SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure the workspace to the target tree (D-01, D-03, D-09)</name>
  <reversibility rating="costly">D-01 — a later restructure rewrites every import path, the turbo task graph, and both Dockerfiles once real code exists; CONTEXT.md records the developer choosing to pay it now.</reversibility>
  <files>package.json, pnpm-workspace.yaml, .gitignore, packages/fastapi-server/pyproject.toml, packages/fastapi-server/package.json, packages/fastapi-server/README.md</files>
  <read_first>
    - package.json (root — currently named "template")
    - pnpm-workspace.yaml (currently `apps/*` + `packages/*`)
    - turbo.json (confirm no task-definition change is needed — see PATTERNS.md "turbo task graph is already D-08-shaped")
    - .gitignore
    - packages/fastapi-server/pyproject.toml (the blog dependencies being stripped)
    - packages/fastapi-server/package.json (the `db:*` alembic scripts being stripped)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-PATTERNS.md (sections: `turbo.json, pnpm-workspace.yaml, root package.json`)
  </read_first>
  <action>
Set root `package.json` `"name"` to `"quant-platform"`. Leave the `scripts` block unchanged —
PATTERNS.md confirms `turbo run build|dev|lint|check-types|test` is already exactly D-08's shape.
Leave `turbo.json` unchanged for the same reason: the `^lint` / `^build` / `test` wiring is
package-type agnostic, so no new task definitions are required and adding some would be drift.

Replace `pnpm-workspace.yaml`'s `packages:` list with exactly four entries, in this order:
`"packages/*"`, `"engine"`, `"prisma"`, `"schemas"`. Removing `"apps/*"` is D-09 — `apps/web` and
`apps/server` leave the workspace until Phase 4 so nothing dormant needs maintaining. Do NOT
delete the `apps/` directories from disk; they stay in git as the Phase 4 starting point and as
the source `apps/server/prisma/` is copied from in plan 01-02. `prisma/` and `schemas/` do not
exist yet — they are created by plans 01-02 and 01-04; pnpm resolves a missing workspace entry to
zero packages without erroring, which is why listing them now is safe.

Append to `.gitignore`, under a `# Generated contract artifacts (D-06)` heading:
`engine/generated/`, `schemas/generated/`. Under a `# Backtest run outputs (D-19, D-20)` heading:
`runs/`, `*.png`. Under a `# Local data catalog cache (D-23)` heading: `catalog/`, `.data-cache/`.
D-06 makes the generated Pydantic and TypeScript directories build outputs — they must never be
committed, and no downstream plan may list a file under them as an artifact.

Strip the fastapi-blog domain from `packages/fastapi-server` per D-03, keeping the package as the
reserved Phase 5 supervisor surface for the per-tenant LiveNode processes. In its
`pyproject.toml`: rename the project to `supervisor`, remove the `aiosqlite`, `alembic`,
`fastapi[standard]` extras beyond plain `fastapi`, `pwdlib[argon2]`, `pydantic-settings`, `pyjwt`
and `sqlalchemy` dependencies (leave `fastapi` itself and the dev group), and delete the
`[tool.ruff.lint.per-file-ignores]` entry keyed on the alembic directory since that directory is
going. In its `package.json`: delete every `db:*` script (migrations belong to the Prisma package
per D-11, not here). Delete the blog routers, models, auth modules, alembic directory and the
package's own docker-compose file; D-10 replaces the latter with one root compose. Rewrite the
package README to one paragraph saying the package is reserved for Phase 5 and has no Phase 1 role.

Do not touch `apps/server/prisma/` in this task — plan 01-02 owns the extraction.
  </action>
  <verify>
    <automated>node -e "const w=require('fs').readFileSync('pnpm-workspace.yaml','utf8'); if(/apps\/\*/.test(w)) process.exit(1); if(!/[\"']?engine[\"']?/.test(w)) process.exit(1); console.log('workspace ok')"</automated>
    <fails_when>non-zero exit, or the string "workspace ok" is absent from stdout</fails_when>
    <automated>grep -v '^#' .gitignore | grep -c 'engine/generated/'</automated>
    <fails_when>prints 0</fails_when>
    <automated>grep -v '^#' packages/fastapi-server/pyproject.toml | grep -cE 'aiosqlite|alembic|pwdlib|pyjwt'</automated>
    <fails_when>prints anything other than 0</fails_when>
    <automated>pnpm install --lockfile-only</automated>
    <fails_when>non-zero exit, or stderr contains "ERR_PNPM"</fails_when>
  </verify>
  <acceptance_criteria>
    - Root `package.json` `"name"` is the literal `quant-platform`, not `template`.
    - `turbo.json` is byte-identical to its pre-task content (no task definitions added).
    - `pnpm-workspace.yaml` `packages:` contains exactly `packages/*`, `engine`, `prisma`, `schemas` and no `apps/*`.
    - `.gitignore` contains lines `engine/generated/`, `schemas/generated/`, `runs/`, `catalog/`.
    - `git ls-files apps/web apps/server` still returns files — the directories were de-listed from the workspace, not deleted from git.
    - `packages/fastapi-server/package.json` has no key starting with `db:`.
  </acceptance_criteria>
  <done>The workspace resolves to packages/*, engine, prisma and schemas; the blog domain is gone from the reserved FastAPI package; the apps directories remain in git but out of the workspace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create the `engine/` uv project, its turbo shim, and the pytest harness (D-02, D-08)</name>
  <reversibility rating="one-way">D-02 — success criterion 3 greps the literal path `engine/dsl/`, so this directory name is a published acceptance criterion and every downstream phase's lock-in check is anchored to it. CONTEXT.md records the developer choosing this exact layout; the rating is recorded, not re-asked.</reversibility>
  <files>engine/pyproject.toml, engine/package.json, engine/uv.lock, engine/README.md, engine/dsl/__init__.py, engine/adapters/__init__.py, engine/data/__init__.py, engine/cli/__init__.py, engine/persistence/__init__.py, engine/tests/__init__.py, engine/tests/conftest.py, engine/tests/dsl/__init__.py, engine/tests/dsl/test_scaffold_smoke.py</files>
  <read_first>
    - packages/fastapi-server/pyproject.toml (the toolchain shape to copy: requires-python, [tool.pytest.ini_options], [tool.ruff] line-length 120 / target-version py313, [tool.ruff.lint] select E,F,I,UP,B,SIM,C4)
    - packages/fastapi-server/package.json (the `package.json`-as-turbo-shim idiom, including the repo's `check-types` convention)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-PATTERNS.md (sections: `engine/pyproject.toml`, `engine/package.json`)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md (`## Standard Stack` for the exact package set and `## Validation Architecture` for the Wave 0 gaps this task closes)
  </read_first>
  <behavior>
    - Test 1: `uv run --project engine pytest` collects and passes at least one test.
    - Test 2: the `synthetic_bars` fixture yields a list of plain dataclass rows with `open`, `high`, `low`, `close`, `volume`, `ts_event` — no engine package involved, so `engine/tests/dsl` is runnable in an environment where the trading engine is not installed.
    - Test 3: every `ts_event` in the fixture is strictly increasing, so any later close-time assertion (DATA-06) has an unambiguous baseline.
  </behavior>
  <action>
Create `engine/pyproject.toml` copying the toolchain shape from the fastapi-server analog and
none of its dependencies: `name = "engine"`, `requires-python = ">=3.13"`,
`[tool.pytest.ini_options]` with `testpaths = ["tests"]` and
`filterwarnings = ["ignore::DeprecationWarning"]`, `[tool.ruff]` with `line-length = 120` and
`target-version = "py313"`, `[tool.ruff.lint]` with `select = ["E","F","I","UP","B","SIM","C4"]`
and `ignore = ["E501"]`. Do not copy the analog's `asyncio_mode`/`asyncio_default_fixture_loop_scope`
settings — nothing in this engine is async — and do not copy its per-file-ignores for the alembic
directory, which does not exist here.

Declare runtime dependencies exactly as RESEARCH.md's Standard Stack specifies, and no others:
`pydantic`, `sqlalchemy`, `psycopg[binary]`, `pyarrow`, `matplotlib`. Declare the dev group as
`pytest`, `ruff`, `datamodel-code-generator`. `polars` is deliberately omitted: RESEARCH.md's own
Standard Stack row calls it "not strictly required for 1h bars at a handful of majors" and
recommends the `csv` stdlib plus direct bar construction, which is what plan 01-05 uses — do not
add a parsing dependency the data volume does not justify. `import-linter` is deliberately
omitted: the acceptance criterion is a literal grep, and a second differently-shaped check would
drift from what the roadmap promises. The trading engine itself is deliberately absent here — plan
01-03 adds it as an editable path dependency on the pinned submodule (D-04), and this task must
leave `engine/pyproject.toml` resolvable without it so that plan 01-08's engine-free CI job has
something coherent to build from.

Create `engine/package.json` as the turbo shim, matching the fastapi-server idiom:
`"name": "@repo/engine"`, `"private": true`, and scripts `"build": "uv sync --frozen"`,
`"lint": "uv run ruff check ."`, `"format": "uv run ruff format ."`,
`"check-types": "uv run ruff check --select F ."`, `"test": "uv run pytest"`. Reuse the repo's
existing `check-types` convention verbatim rather than introducing mypy or pyright, which D-08
does not ask for.

Deliberately do NOT declare a `dependencies` block on `@repo/schemas` in this task, even though the
engine will need it: the `schemas/` workspace member does not exist until plan 01-04 creates it,
and a `workspace:*` reference to a package with no `package.json` makes `pnpm install` fail here in
wave 1. Plan 01-04 Task 2 adds that dependency edge in the same commit that creates the package it
points at. That edge is load-bearing rather than cosmetic — it is what makes Turbo's `^build`
ordering run the codegen step before the engine's tests collect on a clean checkout, which is the
gap the cross-AI review found (see the review dispositions recorded in plan 01-04).

Create the five package directories with `__init__.py` files: `engine/dsl/`, `engine/adapters/`,
`engine/data/`, `engine/cli/`, `engine/persistence/`. `engine/dsl/__init__.py` must stay empty of
any import of the trading engine forever — write no comment naming that module inside
`engine/dsl/`, because the purity gate is a bare recursive content grep over this directory and a
comment mentioning the module makes the gate self-invalidating. State the rule in
`engine/README.md` instead, which is outside the grepped path.

Create `engine/tests/conftest.py` with a `synthetic_bars` fixture: a module-level frozen dataclass
`SyntheticBar(open: str, high: str, low: str, close: str, volume: str, ts_event: int)` holding
decimal strings, and a fixture returning a deterministic list of at least 40 such bars whose
`close` values form a shape that crosses a 5-period and a 20-period simple moving average at
least once in each direction. Timestamps are nanoseconds, strictly increasing, one hour apart.
The fixture imports nothing outside the standard library.

Create `engine/tests/dsl/test_scaffold_smoke.py` asserting the fixture's length, its strictly
increasing timestamps, and that every price field parses as `decimal.Decimal` without going
through `float`.

Run `uv sync --project engine` to produce `engine/uv.lock`, and commit the lockfile.
  </action>
  <verify>
    <automated>uv run --project engine pytest engine/tests/dsl -q</automated>
    <fails_when>non-zero exit, or the summary line contains "no tests ran" or "0 passed"</fails_when>
    <automated>uv run --project engine ruff check engine</automated>
    <fails_when>non-zero exit, or stdout contains "error:"</fails_when>
    <automated>test -f engine/uv.lock &amp;&amp; test -d engine/dsl &amp;&amp; test -d engine/adapters</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>grep -rl nautilus_trader engine/dsl/ ; test $? -eq 1</automated>
    <fails_when>non-zero exit from the wrapper, meaning grep found at least one match under engine/dsl/</fails_when>
  </verify>
  <acceptance_criteria>
    - `engine/pyproject.toml` contains the literal `line-length = 120` and `target-version = "py313"`.
    - `engine/pyproject.toml` declares no dependency whose name matches `polars` or `import-linter`.
    - `engine/package.json` `scripts.test` is the literal `uv run pytest`.
    - `engine/tests/conftest.py` defines a fixture named `synthetic_bars` returning at least 40 items.
    - `uv run --project engine pytest engine/tests/dsl -q` passes with at least 3 assertions collected.
    - `grep -rl nautilus_trader engine/dsl/` prints nothing (exit 1).
  </acceptance_criteria>
  <done>The engine uv project builds, ruff is clean, pytest collects and passes the scaffold suite, and the purity grep already returns empty on the newly created directory.</done>
</task>

</tasks>

<!-- planner-discipline-allow: nautilus_trader -->

<review_dispositions>
Findings from `01-REVIEWS.md` (cross-AI review, 2026-09-06) that landed on this plan.

| Finding | Reviewer | Disposition |
|---|---|---|
| Fresh-clone code generation is not wired into the dependency graph — the engine build script is only `uv sync --frozen` (`01-01-PLAN.md:270`), so a clean checkout can fail before tests collect | Codex (HIGH, blocker 3) | **Accepted, fixed in plan 01-04.** Verified: the review is right — `turbo.json:33` makes `test` depend on `^build`, but that edge only fires if a package dependency exists, and none did. The fix is the `@repo/engine → @repo/schemas` workspace dependency, not committing the generated models: D-06 gitignores those directories and committing them would reverse a binding decision. This plan records *why* the edge is not declared here (the `schemas/` package does not exist in wave 1) and points at plan 01-04 Task 2, which declares it alongside a clean-checkout proof. |
| OpenCode: verify the Rust toolchain is available in plan 01-01 or early in 01-03 | OpenCode (HIGH, uncited) | **Rejected as already handled.** Plan 01-03 Task 1 is a `blocking-human` legitimacy gate over the build backend and the commit pin, and plan 01-03 Task 2 carries the precondition `rustup show` reports an installed toolchain and `cargo --version` succeeds on PATH. Adding a second toolchain check in wave 1, before anything Rust-shaped is even fetched, would be a check with nothing to protect. |
</review_dispositions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → developer machine | PyPI and npm packages are executed at install and at every test run |
| repository → CI runner | workspace and lockfile contents determine what CI installs and runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | PyPI/npm installs in Task 3 | high | mitigate | Package-legitimacy gate (Task 1) is a blocking human checkpoint over all twelve `[SUS]`-flagged packages before the first install; `engine/uv.lock` and `pnpm-lock.yaml` are committed so resolution is pinned |
| T-01-01 | Tampering | `.gitignore` generated-dir rules | medium | mitigate | Generated Pydantic/TS directories are ignored (D-06), so a tampered or stale generated model cannot enter the repo and be mistaken for the source of truth; the schema is the only committed contract |
| T-01-02 | Information Disclosure | stripped fastapi-blog auth modules | low | accept | The removed modules held only template credentials that were never real; deleting them removes the surface entirely rather than leaving dormant auth code to rot until Phase 4 |
</threat_model>

<verification>
- `pnpm install` resolves four workspace globs and never touches apps/web or apps/server.
- `uv run --project engine pytest` is green.
- `grep -rl nautilus_trader engine/dsl/` prints nothing.
- `git ls-files apps/` still returns the Phase 4 starting point.
</verification>

<success_criteria>
- `engine/` exists as a resolvable uv project with the five subpackages and a passing test suite.
- The workspace no longer contains `apps/*`.
- No generated contract artifact can be committed.
</success_criteria>

<artifacts_this_phase_produces>
Symbols and paths created by this plan:

| Kind | Symbol / path |
|---|---|
| directory | `engine/` (uv project root) |
| directory | `engine/dsl/`, `engine/adapters/`, `engine/data/`, `engine/cli/`, `engine/persistence/`, `engine/tests/` |
| file | `engine/pyproject.toml` — project name `engine`, `[tool.pytest.ini_options]`, `[tool.ruff]` |
| file | `engine/package.json` — package name `@repo/engine`; scripts `build`, `lint`, `format`, `check-types`, `test` |
| file | `engine/uv.lock` |
| dataclass | `SyntheticBar(open, high, low, close, volume, ts_event)` in `engine/tests/conftest.py` |
| pytest fixture | `synthetic_bars` in `engine/tests/conftest.py` |
| gitignore rules | `engine/generated/`, `schemas/generated/`, `runs/`, `*.png`, `catalog/`, `.data-cache/` |
| renamed | root `package.json` `name`: `template` → `quant-platform` |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-01-SUMMARY.md` when done
</output>
