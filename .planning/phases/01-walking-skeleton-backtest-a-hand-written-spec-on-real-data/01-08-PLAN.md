---
phase: 01-walking-skeleton-backtest-a-hand-written-spec-on-real-data
plan: 08
type: execute
wave: 4
depends_on: ["01-05"]
files_modified:
  - engine/dsl/bars.py
  - engine/dsl/evaluator.py
  - engine/adapters/dsl_strategy.py
  - engine/README.md
  - engine/tests/dsl/test_evaluator.py
  - engine/tests/dsl/test_purity_boundary.py
  - .github/workflows/dsl-purity.yml
autonomous: true
requirements: [FOUND-05, FOUND-06, SIM-04]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`grep -rl nautilus_trader engine/dsl/` returns nothing, enforced by a CI job rather than by convention (success criterion 3, FOUND-06)"
    - "The evaluator test suite runs to completion in under one second in an environment where the trading engine is not installed at all — proving absence, not merely avoidance (success criterion 3, FOUND-05)"
    - "Exactly one non-test module in the repository imports **both** the evaluator and the trading engine, and it is the adapter (FOUND-06, SIM-04). Modules that import only the trading engine — the catalog, the bulk loader, the CLI runner — are expected and are not violations; the invariant is the dual import, which is what makes the adapter the single swap point for paper and live"
    - "When the fast and slow moving averages are exactly equal on a bar, no signal is emitted — a cross requires a strict sign change between the previous bar and the current one"
    - "The evaluator over an empty bar list emits no signals, and over fewer bars than the slow window it emits no signals rather than raising"
    - "Signals are emitted in bar-timestamp order; two bars sharing an event timestamp are processed in input order and that order is stable across runs"
    - "Two evaluator instances built from the same spec and fed the same bars interleaved produce identical signal sequences — no module-level or class-level mutable state is shared between instances"
    - "The evaluator never reads a bar it has not yet been given, so no lookahead is expressible through this code path"
  artifacts:
    - path: "engine/dsl/evaluator.py"
      provides: "hardened pure evaluator with defined equality, empty-input, warmup and ordering semantics"
      exports: ["DslEvaluator", "Signal"]
      min_lines: 80
    - path: "engine/tests/dsl/test_evaluator.py"
      provides: "the sub-second, engine-free evaluator suite success criterion 3 names"
      min_lines: 90
    - path: "engine/tests/dsl/test_purity_boundary.py"
      provides: "the in-suite purity assertions — the empty grep over the pure directory, and the dual-import set being exactly the adapter"
    - path: ".github/workflows/dsl-purity.yml"
      provides: "the CI job that runs the evaluator suite in an environment with no trading engine installed"
      contains: "dsl"
  key_links:
    - from: ".github/workflows/dsl-purity.yml"
      to: "engine/tests/dsl/"
      via: "the job builds a virtual environment without the engine path dependency and runs only the dsl suite"
      pattern: "engine/tests/dsl"
    - from: "engine/adapters/dsl_strategy.py"
      to: "engine/dsl/bars.py"
      via: "the adapter converts each engine bar into a PlainBar before calling the evaluator; the evaluator never sees an engine type"
      pattern: "PlainBar"
  prohibitions: []
---

<objective>
Harden the pure evaluator's edge semantics and make the purity boundary a CI gate rather than a
convention.

Purpose: success criterion 3 has two halves and only one of them is a grep. The other half —
"the evaluator suite runs against a plain list of bars in under a second with no engine present" —
is not provable by a test that merely avoids importing the engine while the package sits importable
in the same environment. That proves avoidance. Proving absence needs a separate environment where
the package genuinely is not installed, and that is a CI job, not a test.
Output: a defined-at-the-edges evaluator and a CI job that fails if either half of criterion 3
regresses.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-05-SUMMARY.md
@engine/dsl/evaluator.py
@engine/dsl/bars.py
@engine/adapters/dsl_strategy.py
@engine/tests/conftest.py
</context>

<flagged_assumptions>
The `SIM-04` probe row is `unclassified` and remains unresolved; it is surfaced as a flagged
planner assumption in plan 01-05 and is not double-counted here. The `FOUND-05` and `FOUND-06`
probe rows were classified and are authored into `must_haves.truths` above.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the evaluator's edge semantics (FOUND-05)</name>
  <files>engine/dsl/bars.py, engine/dsl/evaluator.py, engine/tests/dsl/test_evaluator.py</files>
  <read_first>
    - engine/dsl/evaluator.py (the tracer's evaluator from plan 01-05 — this task defines its edges)
    - engine/dsl/bars.py (the PlainBar seam)
    - engine/dsl/spec.py (plan 01-04 — the spec fields the evaluator reads)
    - engine/tests/conftest.py (plan 01-01 — the synthetic_bars fixture this suite builds on)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md (D-26 — the DSL surface stays bounded to the crossover)
  </read_first>
  <behavior>
    - Test 1 (adjacency): when the fast and slow averages are exactly equal on a bar, no signal is emitted; a signal requires a strict sign change of `fast - slow` between the previous bar and the current one.
    - Test 2 (adjacency): a sequence that touches equality and returns to the same side emits no signal at all — touching is not crossing.
    - Test 3 (adjacency): a sequence that touches equality and continues through to the other side emits exactly one signal, on the bar where the sign genuinely changed.
    - Test 4 (empty): an empty bar list yields an empty signal sequence and raises nothing.
    - Test 5 (empty): a single bar yields no signal.
    - Test 6 (empty): fewer bars than the slow window yields no signal and raises nothing — the warmup period is silent, not an error.
    - Test 7 (empty): the bar exactly at the slow-window length is the first bar that may produce a signal.
    - Test 8 (ordering): signals come back in bar-timestamp order for an ascending input.
    - Test 9 (ordering): two bars sharing an event timestamp are processed in input-list order, and repeating the run yields the identical sequence.
    - Test 10 (concurrency): two evaluator instances built from the same spec, fed the same bars interleaved bar-by-bar, produce identical signal sequences.
    - Test 11 (no lookahead): an evaluator fed bars one at a time produces the same sequence as one fed the whole list, proving no decision depends on a bar not yet delivered.
    - Test 12 (determinism of arithmetic): all moving-average arithmetic is `Decimal`; the suite asserts no intermediate value is a float.
  </behavior>
  <action>
Define the crossover rule as a strict sign change of `fast - slow` between the previous evaluated
bar and the current one. Equality emits nothing, on either the entry or the exit side. This is the
only defensible reading: a moving-average pair that touches and bounces back has not crossed, and
treating equality as a cross makes the strategy fire on a condition that is an artifact of decimal
scale rather than of price. Store the previous comparison's sign, not the previous averages, so the
rule is expressed once and cannot drift between the entry and exit branches.

Define the warmup period explicitly: until the evaluator has seen as many bars as the slow window,
it emits nothing and raises nothing. An empty list, a single bar, and a short list are all the
silent case. The first bar that may produce a signal is the one at exactly the slow-window length.

Hold all state on the instance — a bounded deque of closes plus the previous sign — and nothing at
module or class level. Two evaluators built from the same spec must not be able to influence each
other, which is what makes the evaluator safe to run in the same process as another and what
Test 10 pins down.

Keep every arithmetic value a `Decimal` throughout. The moving average of decimal closes is a
decimal division, and dividing through a float for convenience would put a float on a price path
in the one module the whole architecture exists to keep clean.

The evaluator continues to import only the standard library, `engine.dsl.spec` and `engine.money`.
No comment or docstring under `engine/dsl/` may name the trading engine's module: the purity gate
is a bare recursive content grep over this directory, so a mention in prose would make the gate
report a violation that does not exist. The explanation belongs in `engine/README.md`, which is
outside the grepped path — Task 2 writes it there.

Do not extend the DSL. D-26 bounds the Phase 1 surface to exactly what the crossover spec needs,
and CONTEXT.md records the developer considering a second structurally different spec and dropping
it. This task defines edges on the existing surface; it does not add expressiveness.
  </action>
  <verify>
    <automated>uv run --project engine pytest engine/tests/dsl -q</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 12 passed</fails_when>
    <automated>uv run --project engine pytest engine/tests/dsl -q --durations=0</automated>
    <fails_when>the reported total duration is 1 second or greater — success criterion 3 requires the suite to run in under a second</fails_when>
    <automated>grep -cE '\bfloat\(' engine/dsl/evaluator.py engine/dsl/bars.py</automated>
    <fails_when>any printed line ends in a count other than ":0"</fails_when>
    <automated>uv run --project engine ruff check engine/dsl</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `engine/dsl/evaluator.py` stores the previous comparison sign, not the previous average values, and expresses the cross rule once for both the entry and the exit side.
    - Exact equality of the two averages emits no signal, asserted by a passing test.
    - Empty input, a single bar, and a short list each emit no signal and raise nothing, asserted by passing tests.
    - Two evaluator instances fed the same bars interleaved produce identical signal sequences, asserted by a passing test.
    - Feeding bars one at a time and feeding the whole list produce identical sequences, asserted by a passing test.
    - Neither `engine/dsl/evaluator.py` nor `engine/dsl/bars.py` contains a `float(` call.
    - `pytest engine/tests/dsl` reports 12 or more passing tests and completes in under one second.
    - `engine/dsl/evaluator.py` holds no module-level or class-level mutable state.
  </acceptance_criteria>
  <done>Every edge of the crossover rule is defined and tested — equality, warmup, empty input, tie ordering and instance isolation — with the suite still under a second.</done>
</task>

<task type="auto">
  <name>Task 2: Make the purity boundary a CI gate, in an environment with no engine installed (FOUND-06, SIM-04)</name>
  <files>engine/tests/dsl/test_purity_boundary.py, .github/workflows/dsl-purity.yml, engine/README.md, engine/adapters/dsl_strategy.py</files>
  <read_first>
    - engine/adapters/dsl_strategy.py (the single dual-import file this gate protects)
    - engine/pyproject.toml (the dependency groups; the engine path dependency must be excludable)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-RESEARCH.md (`## Don't Hand-Roll` — why the literal grep beats an import-linter rule, and `### Wave 0 Gaps` — the engine-free job)
    - .planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-CONTEXT.md (`<specifics>` — build the grep into CI, not into a checklist)
  </read_first>
  <action>
Write `engine/tests/dsl/test_purity_boundary.py` with two assertions, and get the second one right
— the obvious phrasing of it is wrong and would fail on correct code.

First: the literal recursive content grep over the pure directory returns no files.

Second: among non-test modules under the engine tree that import the trading engine, the set that
*also* imports `engine.dsl.evaluator` is exactly `{engine/adapters/dsl_strategy.py}`. Do **not**
assert that only one non-test module imports the trading engine at all — three others must. The
catalog needs the parquet catalog type, the bulk loader constructs bars, and the CLI runner
configures the backtest node; all three are legitimate and expected. FOUND-06's actual claim is that
`DslStrategy` is the only file importing both `DslEvaluator` and the trading engine, and that is
the property worth defending: it is why the same evaluator code path runs in backtest, paper and
live with one file to swap. A second dual-import file would quietly end that without breaking any
behaviour test, which is what this assertion exists to catch.

Use the literal grep, not an import-boundary linter. Success criterion 3 *is* the grep, so a
second differently-shaped check would drift from what the roadmap promises, and it would add a
dependency whose only job is to restate an acceptance criterion that already exists.

Write `.github/workflows/dsl-purity.yml`: a job that creates a virtual environment from the engine
project **excluding** the vendored engine path dependency, asserts that importing the trading
engine raises a module-not-found error, and then runs only the pure suite with a wall-clock timeout
of one second on the test phase. This is the half of criterion 3 the in-suite test cannot prove.
A test that merely avoids an import while the package sits importable in the same environment
demonstrates avoidance; only an environment where the package genuinely is not installed
demonstrates absence, and absence is what the criterion claims. The job also runs both checks — the
empty grep over the pure directory and the dual-import set equalling the adapter — so a regression
fails CI even if someone deletes the in-suite assertions. Encode the second check in the workflow
in the same corrected form as the test: filter to non-test modules importing the trading engine,
then intersect with those importing the evaluator, and compare that set against the adapter path. A
bare count of trading-engine importers would fail this repository's own correct code.

Do not build the vendored engine in this job. Excluding it is the entire point, and it also makes
this the fastest job in CI — which matters, because a purity gate that takes as long as the Rust
build is a purity gate someone will eventually make non-blocking.

Write `engine/README.md` stating the boundary rule in prose: the pure directory may not import the
trading engine, the adapter is the only file that may import both, the enforcement is the literal
grep in the workflow, and — critically — no comment or docstring inside the pure directory may name
the forbidden module, because the gate is a bare content grep and prose about the rule would trip
it. This README is outside the grepped path, which is exactly why the explanation lives here.

Review `engine/adapters/dsl_strategy.py` and move any decision logic that leaked into it back into
the evaluator. The adapter translates and acts; it does not decide. Anything in it that reads a
price and produces a judgement belongs on the pure side, where it is testable in a second with no
engine present.
  </action>
  <verify>
    <automated>uv run --project engine pytest engine/tests/dsl/test_purity_boundary.py -q</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 2 passed</fails_when>
    <automated>grep -rl nautilus_trader engine/dsl/ ; test $? -eq 1</automated>
    <fails_when>non-zero exit from the wrapper, meaning grep matched at least one file under engine/dsl/</fails_when>
    <automated>test "$(grep -rl nautilus_trader engine/ --include='*.py' | grep -v '^engine/tests/' | xargs grep -l 'engine\.dsl\.evaluator')" = "engine/adapters/dsl_strategy.py"</automated>
    <fails_when>non-zero exit, meaning the set of non-test engine modules importing BOTH the trading engine and the evaluator is not exactly the adapter — either a second dual-import file appeared, or the adapter stopped importing one of the two</fails_when>
    <automated>grep -c 'engine/tests/dsl' .github/workflows/dsl-purity.yml</automated>
    <fails_when>prints 0</fails_when>
    <automated>grep -c 'dsl.evaluator' .github/workflows/dsl-purity.yml</automated>
    <fails_when>prints 0, meaning the CI job checks only the pure-directory grep and never checks the dual-import property FOUND-06 actually claims</fails_when>
    <automated>grep -v '^#' engine/README.md | grep -c 'grep'</automated>
    <fails_when>prints 0, meaning the boundary rule and its enforcement mechanism are not written down outside the grepped path</fails_when>
  </verify>
  <acceptance_criteria>
    - `engine/tests/dsl/test_purity_boundary.py` asserts both the empty grep over the pure directory and that the dual-import set — non-test modules importing the trading engine *and* the evaluator — is exactly `engine/adapters/dsl_strategy.py`.
    - Neither the test nor the workflow asserts a bare count of trading-engine importers; the catalog, the bulk loader and the CLI runner import it legitimately and must not fail the gate.
    - `.github/workflows/dsl-purity.yml` builds an environment without the vendored engine, asserts the engine import fails there, and then runs the pure suite.
    - The workflow runs both the literal grep and the dual-import check independently of the in-suite assertions.
    - The workflow performs no Rust build.
    - `engine/README.md` states the boundary rule, names the grep as the enforcement, and states that the forbidden module name must not appear in any comment inside the pure directory.
    - `engine/adapters/dsl_strategy.py` contains no comparison of price values and no signal derivation — it translates bars and acts on returned signals only.
  </acceptance_criteria>
  <done>Success criterion 3 is enforced by CI in both halves: the grep is empty, and the pure suite passes in an environment where the engine genuinely is not installed.</done>
</task>

</tasks>

<!-- planner-discipline-allow: nautilus_trader -->

<review_dispositions>
Findings from `01-REVIEWS.md` (cross-AI review, 2026-09-06) that landed on this plan.

| Finding | Reviewer | Disposition |
|---|---|---|
| **Blocker 2.** The one-Nautilus-importer invariant is impossible as written; FOUND-06 requires `DslStrategy` to be the sole file importing **both** the evaluator and Nautilus, not the sole Nautilus importer in the engine | Codex (HIGH) | **Accepted, fixed here as well as in plan 01-05** — this plan owns the CI form of the gate, and leaving the workflow encoding the old invariant would have made every green tracer run fail the purity job. Verified against plan 01-05's Task 1 action, which requires `engine/data/catalog.py`, `engine/data/binance_vision.py` and `engine/cli/backtest.py` to import the trading engine. Task 2 now specifies both checks in Codex's split form — pure directory free of any reference, and the dual-import set equal to the adapter — in the in-suite test, in the workflow, and in the acceptance criteria, with an explicit instruction not to write the bare-count form because it would fail correct code. |
| **`fast_period < slow_period` cannot be a JSON Schema constraint** | Codex (HIGH, fixed in 01-04) | **Threat register updated here.** T-01-33's mitigation previously claimed the schema constrains fast below slow. It now names the real mechanism — the semantic validator in `load_spec` — so the DoS mitigation for unbounded window lengths rests on something that actually runs. |
</review_dispositions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec values → evaluator arithmetic | window lengths and sizing come from an untrusted JSON file |
| pure module ↔ engine module | the architectural boundary the whole lock-in argument rests on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-32 | Tampering | boundary erosion between the pure and adapter layers | high | mitigate | The literal grep plus the exactly-one-dual-import-file count run both in the test suite and in a dedicated CI job, so removing one does not remove enforcement |
| T-01-33 | Denial of Service | unbounded window lengths from a spec file | medium | mitigate | The schema constrains both window lengths to positive integers, and `load_spec`'s semantic validator rejects a fast window not strictly below the slow one (plan 01-04 — the ordering rule is semantic because draft 2020-12 cannot express a cross-property comparison); the evaluator's deque is bounded by the slow window, so a spec cannot cause unbounded memory growth |
| T-01-34 | Tampering | decision logic drifting into the adapter | medium | mitigate | An acceptance criterion asserts the adapter contains no price comparison and no signal derivation; logic that migrates there stops being testable without the engine, which is how SIM-04 would erode silently |
| T-01-35 | Information Disclosure | evaluator error messages echoing spec content | low | accept | Phase 1 has one local operator and the spec is hand-written by that operator; message hygiene matters from Phase 4, when specs arrive over an API |
</threat_model>

<verification>
- The pure suite passes in under a second with 12 or more tests.
- The purity grep is empty, and the set of non-test modules importing both the trading engine and the evaluator is exactly the adapter.
- The CI job proves the pure suite passes with the engine absent, not merely unimported, and runs both checks itself.
</verification>

<success_criteria>
- Success criterion 3 is fully met and enforced in CI, in both its grep half and its no-engine-present half.
- `FOUND-05`, `FOUND-06` and `SIM-04` are structurally guaranteed rather than conventionally observed.
</success_criteria>

<artifacts_this_phase_produces>
| Kind | Symbol / path |
|---|---|
| hardened module | `engine/dsl/evaluator.py` — strict sign-change cross rule, explicit warmup, instance-only state |
| test | `engine/tests/dsl/test_evaluator.py` — 12 edge tests, sub-second |
| test | `engine/tests/dsl/test_purity_boundary.py` — empty-grep over the pure directory, and dual-import-set-equals-adapter assertions |
| workflow | `.github/workflows/dsl-purity.yml` — engine-free environment job |
| doc | `engine/README.md` — the boundary rule and why the forbidden name must not appear inside the pure directory |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-walking-skeleton-backtest-a-hand-written-spec-on-real-data/01-08-SUMMARY.md` when done
</output>